# v0.9.10 Audit Follow-up Implementation Plan

## Revalidacao integral no binario atual - 2026-07-19

- O corpus atual passou pelo CLI em copia descartavel: `1692` arquivos,
  `458864` linhas, zero invalidos, zero conflitos e zero falhas.
- SQLite passou `PRAGMA integrity_check=ok` com `96479` registros.
- Esta evidencia confirma os dois slices de importacao sem tocar fontes ou
  banco de producao.

## Fatia de diagnostico do importador - 2026-07-19

- O resultado por arquivo agora separa invalid number, description e date.
- Full rescan rejeitado informa o nome do arquivo e esses contadores; o caso
  `mixed.xlsx` foi validado sem limpar o banco anterior.
- Focados `3/3` em `0.09 s`; CTest `618/618` em `72.23 s`; review final limpo.
- Nenhum schema, layout, menu ou regra de importacao foi alterado.

# Handoff de auditoria - atualizacao 2026-07-19

## Checkpoint causal de reentrancia do workflow - 2026-07-19

- O caso de terminal reentrante removeu `QTest::qWait(50)` e espera o estado
  `WorkflowCommandRunner::Idle` antes de validar que nao houve segunda chamada.
- Build e CTest focados passaram `1/1` em `3.40 s`; o finding inicial de
  formatacao foi corrigido e o review final ficou sem findings.
- Nenhum codigo de producao, schema, menu ou layout foi alterado.
- Contadores: plano original `99.0/100`; divida nova P0 `14/14 = 100.0%`;
  backlog legado `N/A`; fila operacional `6/8 = 75.0%`.

Proxima atividade unica: continuar determinismo causal ou selecionar o proximo
corte local de importacao/SQLite.

## Checkpoint causal do shutdown de preferencias - 2026-07-19

- O teste de persistencia do snapshot final removeu o `sleep_for(20ms)` e a
  thread releaser. `beginShutdown(latest)` e a liberacao do primeiro save
  agora formam a sequencia causal observavel.
- Build do target `ssa_qt_preference_lifecycle_tests` e CTest focado passaram
  `1/1` em `0.64 s`; diff check, clang-format, Semgrep e detect-secrets sem
  findings. Cppcheck nao selecionou arquivo de teste pelo hook.
- Nenhum comportamento de producao, schema, layout ou menu foi alterado.
- Bitbucket confirma o commit `e338874` e a tag `v0.9.12`. O push GitLab foi
  recusado por `invalid_grant`; a sincronizacao em `origin` permanece externa
  e pendente.
- Contadores: plano original `99.0/100`; divida nova P0 `14/14 = 100.0%`;
  backlog legado `N/A`; fila operacional `6/8 = 75.0%`.

Proxima atividade unica: continuar o fechamento local de determinismo ou
importacao/SQLite.

## Gate completo apos o fechamento SQLite - 2026-07-19

- O CTest dev sequencial foi executado apos `normalizeExistingSsaNumbers`
  passar a usar o predicado do dirty ledger.
- Resultado: `621/621` testes passaram, `0` falhas, em `68.12 s`.
- O gate confirma os contratos locais de importacao, rescan, derivadas,
  filtros e integridade SQLite. Nao e evidencia externa de Windows/UNC,
  PowerShell, SMB ou packaging DMG.
- Contadores atuais: plano original `99.0/100`; divida nova P0
  `14/14 = 100.0%`; backlog legado `N/A`; fila operacional `6/8 = 75.0%`.

Proxima atividade unica: priorizar um corte local de alto impacto ou executar a
prova externa Windows/UNC quando houver host e share gravavel.

## Normalizacao SQLite filtrada pelo dirty ledger - 2026-07-19

- O `SELECT` de normalizacao agora usa o predicado do indice parcial de linhas
  nao canonicas. A migracao continua transacional e fail-closed, mas evita
  visitar linhas canonicas quando o ledger esta esparso.
- Benchmark existente: 30 amostras de 250000 linhas, reabertura canonica
  p50/p95 `2.10/3.49 ms`; legacy `2.03/3.81 ms`; primeira passada legacy
  p95 `3.34 s` e RSS p95 ~`104 MB`.
- Validacao: writer/importacao `7/7`, smokes de benchmark `2/2`, review estatico
  limpo. Nenhum schema, layout ou regra de importacao foi alterado.

## Atomicidade incremental entre blocos externos - 2026-07-19

- Foi acrescentado o contrato de 64 fontes validas seguidas por uma fonte XLSX
  invalida no segundo bloco. O primeiro bloco permanece persistido e movido;
  a fonte invalida continua disponivel para retry.
- Resultado validado: 64 insercoes, 64 movimentos, 65 descobertas,
  `PRAGMA integrity_check=ok` e diagnostico causal do segundo bloco.
- CTest focal `5/5` em `0.96 s`; nenhuma mudanca de schema, layout ou regra de
  importacao.

## Checkpoint causal do mapper de importacao - 2026-07-19

- O mapper de planilhas recebeu um checkpoint opcional apos a primeira linha
  mapeada, usado somente para sincronizar o teste de cancelamento. A chamada
  de producao continua sem callback e preserva o contrato existente.
- O teste deixou de usar `QThread::msleep(5)` e agora libera o worker por
  `stop_callback` apos observar o checkpoint real.
- Review automatico, build do target afetado e cinco testes de importacao,
  lote, rescan e cancelamento passaram. CTest focal: `5/5`, `0.64 s`.
- Este slice nao altera schema, regras de dados, consolidacao ou layout e nao
  recebe credito por provas externas de Windows/UNC.

## TypeScale de entrada - 2026-07-19

- **ENTREGUE localmente**: `AppTextField` e `AppCheckBox` usam somente fonte
  pontual por `Theme.fontPointSizeBody`; o texto do checkbox reutiliza a fonte
  do controle.
- O smoke foi ampliado para os dois componentes e preserva fit em altura real.
  REDs de inicializacao foram diagnosticados sem sleeps e o GREEN passou.
- Review: qmlformat, qmllint, clang-format, Semgrep e detect-secrets limpos.
  Theme gallery `1/1` em `0.73 s`; popup/layout `4/4` em `1.81 s`.
- Nao houve mudanca de layout de producao, menus, schema ou banco. Outros
  consumidores TypeScale e provas externas seguem pendentes.

Proxima atividade unica: escolher o proximo consumidor QML parcial ou voltar
ao backlog local de performance/importacao.

## Release de codigo v0.9.12 - 2026-07-19

- `PROJECT_VERSION` foi atualizado para `0.9.12` apos consulta aos dois
  remotes; `0.9.13` nao foi necessario.
- Dev: build, qmllint e CTest `620/620` em `81.07 s`. Release: build e CTest
  `620/620` em `60.45 s` pelo script canonico.
- A correcao de SAM e a espera terminal do teste foram validadas; review e
  hooks staged estao limpos.
- O host falhou em `hdiutil` com `Dispositivo nao configurado`. Nao ha ZIP ou
  DMG 0.9.12 entregue; `build/release` e o artefato local validado.
- Commit `e2b9c28` e tag `v0.9.12` publicados e confirmados nos dois remotes.

Proxima atividade unica: resolver o packaging DMG em host configurado ou
iniciar a proxima pendencia local.

## Fechamento do contrato de execucao de derivadas - 2026-07-19

- **ENTREGUE localmente**: preferencias de importacao chegam ao request de
  derivadas e sao validadas antes da leitura de qualquer fonte.
- `sqlite_busy_wait_ms` controla o busy handler da transacao SQLite de
  derivadas. `rows_per_chunk` permanece o limite do leitor XLSX externo; o
  merger de derivadas conserva deduplicacao e atomicidade entre fontes.
- Testes: `ssa_qt_presentation_tests` e importacao de derivadas, `22/22` em
  `10.36 s`; suite CTest `620/620` em `81.22 s`.
- Review final sem findings em diff check, clang-format, cppcheck, Semgrep
  C/security e detect-secrets. Commit `f7ae4ab` publicado e confirmado nos
  dois remotes.

Working tree dirty somente por artefatos locais preexistentes fora da fatia.
Proxima atividade unica: proxima pendencia local de alto impacto.

## Parametros de importacao conectados ao workflow - 2026-07-19

- **ENTREGUE localmente**: os valores persistidos em `import_execution` agora
  chegam ao request de importacao externa e de rescan, em vez de serem
  ignorados pelo Runner.
- A tela de Preferencias permite ajustar linhas por lote de `1` a `1000` e
  espera SQLite de `0` a `3000 ms`, sempre em passos de `5 ms`. Valores JSON
  fora do contrato sao limitados antes do uso.
- Evidencia: teste Qt de propagacao com `rows_per_chunk=321` e
  `sqlite_busy_wait_ms=125`; build e qmllint passaram; CTest focado `2/2` em
  `10.46 s` e suite completa `619/619` em `72.05 s`.
- Nao houve mudanca de schema SQLite, regras de filtro ou menus. O warning de
  fonte `Both point size and pixel size set` e independente deste slice.
- Estado publicado: commits `0c28720` e `f1f1232`; `HEAD`, `origin/master` e
  `bitbucket/master` confirmados em
  `f1f12324d8d1f0f5c04a08fb737d0c7723687cbc`. Dirty state preexistente
  permanece fora do escopo.

Proxima atividade unica: iniciar a proxima pendencia de alto impacto, mantendo
Windows/UNC como prova externa separada.

## Fechamento operacional da importacao externa - 2026-07-19

- **ENTREGUE e commitado**: o limite de 64 agora e tamanho de bloco, nao
  limite da selecao. O workflow C++ processa arquivos selecionados em ciclos
  de ate 64, cada ciclo com staging e consolidacao atomicos, e agrega o
  resumo sem interromper a selecao completa.
- Evidencia: 65 XLSX selecionados foram processados em 2 blocos, 65 linhas
  inseridas e 65 fontes movidas para `processadas`; teste ponta a ponta passou
  em `0.40 s`. O C++ nao usa leitor Python; a implementacao foi alinhada ao
  contrato observado no `RescanWorker` Python, que fatia a lista antes de
  chamar a operacao de importacao.
- **ENTREGUE e commitado**: `numero_desvios` normaliza inteiros e rotulos
  `Desvio #N` com espacos, trata `sem desvio` como zero e preserva texto
  desconhecido como NULL opcional, evitando falha de writer na planilha toda.
- **ENTREGUE e commitado**: preferencias schema 14 persistem parametros de
  importacao e mantem defaults em arquivos schema 12 sem o bloco novo.
- Review final: diff check, clang-format, cppcheck, Semgrep C/security e
  detect-secrets sem findings. CTest completo passou `619/619` em `70.29 s`.
- Estado de entrega: HEAD `8955a6f`; `origin/master` e `bitbucket/master`
  confirmados no mesmo hash. Working tree segue dirty somente por artefatos
  locais preexistentes fora do slice.
  Nenhuma prova Windows/UNC real e inferida desta rodada.
- Contadores honestos: plano original `99.0/100`; divida nova P0
  `14/14 = 100.0%`; backlog legado `N/A`; fila operacional local `6/8 =
  75.0%`, com a fronteira Windows/UNC externa pendente.

Proxima atividade unica: abrir outro slice local de alto impacto; Windows/UNC
continua dependente de plataforma real.

## Fechamento do incidente de importacao

- O cabecalho `Prazo Limite` e textual e e mapeado para
  `status_execucao_prazo`; `Data Limite` continua sendo `data_limite`.
- Foi provado o caminho completo no workflow com uma SSA ja existente: a
  linha foi atualizada, o status textual foi gravado, a data ISO foi gravada
  e o campo legado de prazo permaneceu intacto.
- O teste de regressao passou `1/1` e o CTest final passou `618/618` em
  `70.92 s`. O erro de `invalid_number=994` do print nao reproduz no binario
  atual; a validacao integral anterior registrou zero invalidos.
- Esta rodada mudou somente teste e documentacao. Nao houve mudanca de
  schema, layout, menu ou regra de filtragem.

## Estado operacional

- Plano original: `99.0/100`.
- Divida nova: `14/14 = 100.0%` no P0 local de importacao.
- Backlog legado: `N/A`, sem denominador ativo.
- Fila operacional: `5/8 = 62.5%`, com Windows/UNC e outras provas externas.
- Proxima atividade unica: selecionar o maior item local restante ou executar
  a prova Windows/UNC real.

## Fechamento do P0 de importacao - 2026-07-19

- **ENTREGUE**: importacao C++ do corpus real em clone isolado: 1692 arquivos,
  458864 linhas validas, zero linhas invalidas e zero falhas.
- Corrigidos somente perfis comprovados: `Prazo Limite` textual, datas em
  formato dia/mes, reprogramacao textual numerada, relatorios auxiliares de
  relacao, export bruto SAM e quatro linhas historicas incompletas de `Todas as
  SSAs`.
- `PRAGMA integrity_check` retornou `ok`; clone terminou com 96479 registros,
  sem schema novo. Testes focados 5/5, build de integracao/CLI e revisao
  estatica sem findings.
- Aceite: plano original `99.0/100`; divida nova `14/14 = 100.0%` para este P0;
  backlog legado `N/A`; fila operacional `5/8 = 62.5%`, ainda com provas
  externas de Windows/UNC.
- O clone foi descartavel. Nenhum banco ou fonte de producao foi alterado.
- CTest final passou `617/617` em `67.61 s`. O codigo esta em `5e3de3b` e o
  fechamento documental no Bitbucket. `origin/master` ficou em
  `428302d` porque o OAuth GitLab retornou `invalid_grant`.

Proxima atividade: executar a prova Windows/UNC real antes de aceitar a fila
externa de plataforma.

## Reconciliacao de fila e regressao visual - 2026-07-19

- `874e31b` foi publicado em `master`, `origin/master` e `bitbucket/master`.
  Ele restaura pixel size em `ActionButton` e aumenta apenas `Importar XLSX` e
  `Preferencias`; nao altera menus, importacao, schema ou SQLite.
- `FILTER-DIFFERENT-SEMANTICS` e `DISTINCT-ADVANCED-TEXT-FILTERS` saem da fila
  ativa e permanecem apenas como historico resolvido. A unica atividade P0 e
  reproduzir `IMPORT-CORPUS-INVALID-NUMBER` com uma celula real rejeitada,
  preservando atomicidade fail-closed.
- Legado nao possui denominador ativo: status `N/A`, nao `0.0/100`.

> **For Claude:** Use `${SUPERPOWERS_SKILLS_ROOT}/skills/collaboration/executing-plans/SKILL.md` to implement this plan task-by-task.

**Goal:** Continuar a estabilizacao pos-v0.9.10 a partir da reauditoria de Cursor GLM e OpenCode, corrigindo primeiro contratos SQL e concorrencia antes da decomposicao dos hubs de importacao.

**Architecture:** Preservar domain e query sem Qt/SQLite, presentation sem SQL e infra sem decisao visual. Findings externos sao evidencia de entrada, nao autoridade; cada slice exige reproducer, patch minimo e contrato focado antes de alterar estrutura.

**Tech Stack:** C++20, Qt 6.11, QML, SQLite, Catch2, QtTest, CMake presets `dev` e `release`.

---

## Gate de estabilizacao v0.9.11 - 2026-07-18

- CMake `dev` reconfigurado e build canonico verde. O warning de miniz e
  preexistente em dependencia cacheada, nao finding do release.
- CTest sequencial completo: `602/602` em `64.93 s`, incluindo GUI/CLI
  `0.9.11`, importacao, SQLite, derivadas, QML, memoria e benchmarks smoke.
- Semgrep amplo (`24` regras/`512` arquivos), Gitleaks (`1.43 GB`) e
  TruffleHog ficaram sem finding. Os hooks staged tambem passaram.
- A tag anotada `v0.9.11^{}` confirma `84462d4`; Bitbucket `master` confirma
  `6a0bfa4`, que registra a publicacao do gate. GitLab recusou push por OAuth
  `invalid_grant`, dependencia externa sem sucesso alegado.

## Recheck pos-tag v0.9.11 - 2026-07-19

- O HEAD `03d71dad` esta doze commits depois de `v0.9.11` em `84462d4`. A tag
  anotada ja existe nos dois remotos e nao foi movida: publicacao posterior
  requer uma nova versao, nao retag do release existente.
- Build `dev` passou; CTest sequencial passou `609/609` em `72.84 s`, incluindo
  os oito contratos do log historico `8/564`. Portanto, aquele log nao e uma
  falha ativa no HEAD atual.
- Semgrep (`24` regras/`512` arquivos), Gitleaks (`714` commits) e TruffleHog
  Git terminaram sem segredo. TruffleHog filesystem saiu `183` em caches locais,
  fixtures XLSX e binarios; nao conta como sucesso nem finding confirmado.
- Contadores fixos seguem plano `99.0/100`, divida `13/14 = 92.9%`, legado
  `0.0/100` e fila `5/8 = 62.5%`.

## Contraste runtime do setor rapido - 2026-07-19

- [RESOLVED-LOCAL] `PagerQuickFilters` tinha o Label `Setor:` em accent direto
  sobre o surface de `SearchAndPager`; 17/39 paletas ficaram abaixo de AA no
  RED runtime. A unica mudanca de producao usa
  `Theme.readableText(Theme.surface, Theme.accent)`.
- O teste instancia o componente real, le a cor resolvida, prova o contexto
  surface e exige as 39 chaves exatas (26 nativas, 13 `*py`). P1 de proxy
  sintetico e P2 de cobertura incompleta foram encontrados e corrigidos antes
  do GREEN; review Terra ultra final `SEM FINDINGS`.
- Gates focados e `all_qmllint` passaram; CTest GREEN `1/1` em `1.99 s`.
  `QML-AA-BINDING-SMOKE` permanece parcial: sete consumidores ainda precisam
  de runtime smoke. Contadores seguem plano `99.0/100`, divida `13/14`, legado
  `0.0/100` e fila `5/8`.

## Sincronizacao remota restaurada - 2026-07-18

- `origin/master` e `bitbucket/master` foram confirmados ate `09cc0c4`; a tag
  anotada `v0.9.11^{}` tambem foi confirmada em `84462d4` nos dois remotos.
- Os registros de OAuth `invalid_grant` acima permanecem no historico, mas nao
  bloqueiam mais a publicacao atual.

## Recovery de selecao externa apos journal - 2026-07-18

- [IMPLEMENTED-LOCAL] O diagnostico encontrou falso sucesso em
  `importExternalFiles`: uma selecao externa podia ser staged, um journal
  anterior ser recuperado, e a selecao atual ser descartada sem SQLite ou
  consolidacao. O patch preserva o primeiro staging para arquivos distintos.
- Replay e identificado somente para candidato de mesmo nome e bytes iguais ao
  snapshot journalizado. A comparacao e cancelavel e falha fechada; remove
  apenas a copia repetida. Mesmo nome com bytes alterados continua como lote
  novo, sem segundo copy/restage.
- Evidencia: RED `1/1` reproduziu `duplicate_conflict`; GREEN focal `7/7` em
  `0.51 s`; familia importacao/SQLite `196/196` em `7.60 s`; formatacao,
  cppcheck, Semgrep e detect-secrets limpos. clang-tidy so listou cinco avisos
  baseline; review Terra ultra final `SEM FINDINGS`.
- Sem schema, layout ou API publica. Contadores permanecem plano `99.0/100`,
  divida `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.
  A prova Windows/UNC real continua externa e sem credito.

## Prazo SQLite configuravel no importador SAM - 2026-07-18

- [IMPLEMENTED-LOCAL] `SamImportRequest` passou a transportar
  `sqliteBusyWait`, com default de 3000 ms de `ImportExecutionOptions`. O SAM
  fixava 3000 ms somente no recovery e antes descartava a configuracao na
  escrita do lote.
- O workflow valida faixa/passo antes de staging e locks e encaminha o mesmo
  prazo a recovery e escrita. O refresh atual preserva 3000 ms; expor uma
  preferencia de usuario fica separado para nao misturar GUI, schema e este
  contrato de confiabilidade.
- RED mostrou 1 ms aceito e 0 ms esperando lock. GREEN causal `3/3` em
  `0.61 s` cobre rejeicao pre-staging, escrita bloqueada e retomada de journal
  sob lock. Familia importacao/SQLite `207/207` em `65.19 s`, com integridade,
  fonte e journal preservados antes do retry.
- Formato, cppcheck, Semgrep (11 regras, zero finding) e detect-secrets
  limpos. clang-tidy nao trouxe warning novo; cinco avisos sao baseline.
  Re-review Terra ultra final: `SEM FINDINGS`. O aviso Ninja de cache nao e
  apresentado como validacao.
- Contadores sem inflacao: plano `99.0/100`, divida `13/14 = 92.9%`, legado
  `0.0/100` placeholder e fila `5/8 = 62.5%`.

## TypeScale em controles compartilhados - 2026-07-18

- [PARTIAL-LOCAL] O primeiro slice adiciona `Theme.fontPointSizeBody` de 9 pt
  e migra apenas `ActionButton`, `AppComboBox`/delegate e `AppSpinBox`. A
  altura implicita preserva 30 como minimo e so cresce para texto maior.
- O RED final provou `pixelSize`. GREEN instancia texto com ascendentes e
  descendentes, exige point sem pixel, abre o delegate e prova que 1.5x chega
  ao popup sem clipping. `ssa_qml_theme_gallery_tests` passou `1/1` em
  `0.79 s`, tambem renderizando 12 paletas em 960/720; duas capturas offscreen
  foram inspecionadas sem defeito visual observado.
- qmlformat, qmllint, Semgrep QML (5 regras), Semgrep C (2 regras) e
  detect-secrets passaram. O hook QML nao gravou em home no sandbox, mas a
  policy equivalente com HOME temporario passou. Review Terra ultra corrigiu
  P2 de escala do delegate e re-review terminou `SEM FINDINGS`.
- Restam 32 QML, AppTextField, AppCheckBox, filtros/cards e plataformas reais
  com fonte Large/HiDPI. Nao atribuir migracao global ou credito novo aos
  contadores: plano `99.0/100`, divida `13/14 = 92.9%`, legado `0.0/100`,
  fila `5/8 = 62.5%`.

Proxima atividade unica: escolher a maior pendencia local antiga com evidencia
suficiente antes de outro corte TypeScale.

## Contrato Windows/UNC executavel - 2026-07-18

- `ssa_windows_unc_import_contract` agora e um CTest externo opt-in: em
  Windows, configurar com `SSA_ENABLE_WINDOWS_UNC_CONTRACT=ON` e
  `SSA_WINDOWS_UNC_TEST_ROOT=\\server\share`. Sem root, CMake falha de modo
  explicito; sem opt-in, o caso `[windows-unc]` fica excluido da descoberta
  normal para nao contaminar a suite local.
- O contrato usa filho temporario owned Unicode, recusa namespaces locais,
  importa dois XLSX no share, valida SQLite e consolidacao e prova que o lock
  do corpus preserva a fonte na mesma maquina/share. A fixture fornece UTF-8
  ao miniz, inclusive em Windows.
- Localmente: review Terra ultra final `SEM FINDINGS`; formatacao, Semgrep e
  detect-secrets limpos; build de integracao, CTest rescan/lock `5/5` em
  `0.16 s` e paths Unicode/multi-sheet `4/4` em `0.36 s`. macOS listou zero
  testes externos, esperado e nao validacao de Windows.
- Falta executar em Windows 11/SMB real e registrar versoes, share e erros
  Win32. Nao atribuir credito ao `IMPORT-STAGER-HUB`; dois hosts, TOCTOU,
  reparse, crash/recovery e URL de grafo UNC continuam pendentes.
- O commit `09cc0c4` publicou o contrato em `origin/master` e
  `bitbucket/master`, ambos conferidos no SHA do HEAD. O hook staged passou
  clang-format, cmake-format, Gitleaks, detect-secrets e TruffleHog.

Proxima atividade unica: executar o CTest Windows/UNC real antes de aceitar a
pendencia externa de importacao.

## Contraste AA contextual QML - 2026-07-18

- [RESOLVED-LOCAL] [THEME-PY-AA] O overload
  `Theme.readableText(background, preferred)` mantem a cor preferida se ela
  atende `4.5:1`, usa os candidatos anteriores quando possivel e cai em preto
  ou branco somente nos casos restantes. As 13 paletas Python nao foram
  alteradas.
- Foram corrigidos os foregrounds contextuais de combo, botoes, tabs, atalhos,
  tema, tabela, analytics e Sobre. O link de analytics considera `surface` no
  hover. O contrato percorre 39 paletas e 37 pares de estado.
- Validacao local: CTest focal `1/1` em `2.30 s`; fechamento QML `5/5` em
  `6.49 s`; `all_qmllint`, qmlformat, clang-format, Semgrep e detect-secrets
  limpos. Review Terra ultra achou P2 no hover e na restauracao do singleton;
  ambos corrigidos, re-review `SEM FINDINGS`.
- `TYPESCALE-POINTSIZE` continua separado: 100 usos de pixel em 36 QML com
  alturas fixas impedem troca mecanica sem validacao visual por plataforma.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

## Recheck UX-GRID-ALIGN - 2026-07-18

- A reproducao offscreen atual em `1580x940` nao reproduziu desalinhamento de
  Macro ou Reprogramacoes. Os cards exibem baseline, padding e altura visual
  coerentes com os cards de texto; nenhum QML foi alterado.
- O CTest `ssa_qml_advanced_popup_tests` passou `1/1` em `1.58 s`. Resta
  reproducer no monitor afetado ou contrato geometrico no mesmo `Flow` antes
  de fechar o item ou alterar layout.

## Analytics atomico na importacao incremental - 2026-07-18

- O commit `44411da` prova o caminho que importa: `rescan` termina em
  `finishWithAnalytics` no mesmo commit SQLite. Um schema invalido de
  `activity_analytics_point` falha com diagnostico especifico de analytics e
  deixa fonte, SSA anterior, journal e tabelas analytics sem publicacao parcial.
- O benchmark manual de 250 mil linhas deixou de chamar projection diretamente.
  Ele executa base, no-op com o mesmo XLSX fisico restaurado e delta de uma
  linha pelo workflow real. A rodada validada resultou em `12239.400 ms`,
  `10338.718 ms` e `2514.052 ms`, respectivamente; contrato
  `250000 -> 250000 -> 250001`, seis snapshots, nove pontos, SPG `250001` e
  RSS adicional `121978880` bytes, abaixo de 256 MiB.
- O gate focado passou `28/28` em `14.67 s`; formatacao, cppcheck, Semgrep e
  detect-secrets passaram. clang-tidy saiu zero sem warning novo do diff, com
  warnings baseline de Catch2 e do arquivo de teste. Bitbucket confirmou
  `44411da`; GitLab `origin` segue OAuth `invalid_grant`.
- O mapeamento Terra ultra cobriu import writer, Derivadas, Maintenance e o
  rescan que publica backup do DB temporario. Um ledger futuro teria de ser
  sidecar no mesmo DB, com triggers DML, bootstrap fail-closed e copia pelo
  backup; `capture` continuaria obrigatorio para data, schema e integridade.
- Nenhum schema foi aprovado: fingerprint historico de 250 mil linhas custa
  `239.838 ms`, aproximadamente `2.32%` do no-op real `10338.718 ms`, antes de
  trigger por linha, DB legado e contratos novos de rollback/publicacao. O
  ganho marginal nao compensa alterar a fronteira atomica atual.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado placeholder `0.0/100`, fila `5/8 = 62.5%`.

Proxima atividade unica: mapear a prova Windows/UNC real do ultimo item de
confiabilidade de importacao, sem alterar schema ou staging especulativamente.

---

## UX-NAV: cadeia fixa e erros concorrentes - 2026-07-18

- Codigo entregue no HEAD `08991b6`; confirmacao externa sera registrada apos
  publicacao. A selecao da tabela captura cadeia e raiz do grafo. Navegar
  troca somente o registro exibido, com IDs/geracao descartando respostas stale.
- Lanes de Record e DirectChildren mantem erro separado, com prioridade Record.
  Assim um sucesso tardio nao mascara erro concorrente. Navigator e janela de
  grafo mostram erro antes de loading; QML passa indice para preservar SSA
  duplicada na cadeia.
- REDs: cadeia que encolhia, filhas canceladas, indice duplicado e os dois
  sentidos de erro concorrente. Review Terra ultra encontrou P1/P2 reais;
  correcao e re-review final `SEM FINDINGS`.
- Validacao: static gates limpos; build afetado; CTest de presentation e QML
  `2/2` em `14.87 s`; `all_qmllint` passou; hook staged passou scanners. Sem
  schema ou layout novo. Contadores inalterados: plano `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100`, fila `5/8 = 62.5%`.
- A sync completa de derivadas segue bloqueada por produto: C++ e escalar, mas
  a fonte externa exige procedencia, matriz, closure, resumo e sync runs.
  Definir prioridade, multiparent, ciclos, orfaos e desativacao antes de DDL.

Proxima atividade unica: gate completo do candidato `0.9.11`; tag apenas se
suite e verificacoes de release estiverem verdes.

---

## Checkpoints causais privados de parsing e merge de derivadas - 2026-07-18

- `77fd3e0` elimina tres waits de `1/10ms` que so observavam futures pendentes.
  O checkpoint de parsing ocorre apos 4 KiB reais de linha delimitada; o de
  merge ocorre apos a primeira edge aceita pelo fluxo completo do port.
- O primeiro design recebeu P2 por expor semaforos na API publica e P1 por
  cobrir merger isolado. O desenho final usa `DerivadasImportTestAccess` apenas
  em testes, overloads privados por chamada e terceiro contrato integrado que
  exige cancelamento, ausencia de escrita e reuso posterior do banco.
- Evidencia local: gates estaticos limpos; build `ssa_integration_tests`;
  causal `3/3` em `0.08 s`, repeticao `90/90` em `1.90 s`, derivadas `22/22`
  em `0.49 s`; dois re-reviews Terra ultra `SEM FINDINGS`. Bitbucket confirma
  `77fd3e0`; GitLab `origin` segue OAuth `invalid_grant`; clawpatch local nao
  iniciou review por tooling/modelo incompativel.
- Nao houve mudanca de schema, SQL, API publica ou regra de derivadas. O limite
  residual e I/O bloqueado dentro de `std::getline`, fora do contrato cooperativo
  provado. Contadores nao mudam: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila operacional `5/8 = 62.5%`.

Proxima atividade unica: diagnosticar a sincronizacao completa de derivadas
pendente contra regras de negocio e fonte externa antes de editar schema ou UI.

---

## Cancelamento causal de derivadas sob SQLite bloqueado - 2026-07-18

- `6e818c0` fecha a lacuna de teste que usava espera fixa antes de cancelar
  derivadas sob lock SQLite. O port recebe `busyEntered` opcional e o handler
  publica uma unica vez no primeiro `SQLITE_BUSY`; a API normal permanece
  inerte sem sinal.
- Importacao e limpeza de orfas agora esperam contencao real, pedem stop,
  fazem ROLLBACK e provam reuso. O RED revelou API ausente e dois detalhes de
  libc++ macOS, corrigidos sem mudar schema, SQL ou regra de negocio.
- Evidencia local: gate estatico limpo; build afetado; causal `2/2` em
  `0.07 s`, repeticao `60/60` em `2.57 s`, derivadas `22/22` em `0.61 s` e
  SQLite Repository `31/31` em `0.76 s`; review Terra ultra `SEM FINDINGS`.
  Bitbucket confirma `6e818c0`; GitLab `origin` segue OAuth `invalid_grant`;
  clawpatch local nao iniciou por tooling incompativel.
- Contadores nao mudam: plano original `99.0/100`, divida nova `13/14 = 92.9%`,
  legado `0.0/100` placeholder e fila operacional `5/8 = 62.5%`.

Proxima atividade unica: separar e tornar causais os waits de parsing e merge
de derivadas, sem acoplar essas fases ao handler SQLite.

## Recovery de staged journalizado antes de rescan - 2026-07-18

- `1d04ab0` fecha um P0 de durabilidade: `rescan()` consulta e retoma o journal
  de consolidacao antes de o stager limpar `.ssa-staged-*`. A ordem anterior
  podia apagar a copia owned de importacao externa apos crash entre commit e
  move, deixando journal sem fonte recuperavel.
- O RED usa o crash probe com source staged e falha no baseline por estado
  ambiguo de source/destination. Apos o patch, o move vai para `processadas/`,
  o journal e removido e os artifacts realmente abandonados continuam limpos.
  A private `importIncrementalFiles` sem call site tambem foi removida.
- Evidencia local: gate estatico limpo; build de `ssa_integration_tests`;
  direto `5/5` em `0.47 s`; workflow `174/174` em `9.43 s`; crash `20x` em
  `1.07 s`; review Terra ultra `SEM FINDINGS`. clang-tidy so repetiu avisos
  preexistentes. Bitbucket confirma `1d04ab0`; GitLab `origin` segue OAuth
  `invalid_grant`; clawpatch local nao iniciou por tooling incompativel.
- Contadores nao mudam: plano original `99.0/100`, divida nova `13/14 = 92.9%`,
  legado `0.0/100` placeholder e fila operacional `5/8 = 62.5%`.

Proxima atividade unica: tornar causal o cancelamento de derivadas sob
contencao SQLite, sem alterar regras de negocio.

## Handshake causal dos crash probes SQLite - 2026-07-18

- `6bcf65e` remove os tres loops que aguardavam marker ou `-journal` nos
  contratos de crash. O probe publica `READY\n` em stdout somente apos concluir
  o marker atomico `QSaveFile`; o teste acumula stdout para leituras parciais e
  mata o filho somente apos o sinal causal.
- O caso `journal-delete-before-commit` obteve um writer observado separado:
  `pendingConsolidation()` ocorre sem semaforo e `busyEntered` so existe em
  `completeConsolidation`. A `jthread` acorda no primeiro busy do DELETE/COMMIT;
  `stop_callback` libera o semaforo no caminho de erro. O contrato continua
  exigindo a existencia de `-journal` antes do kill.
- O RED falhou `3/3` na ausencia do protocolo. Review Terra ultra encontrou P1
  de causalidade de fase, corrigido antes de review final `SEM FINDINGS`.
  Evidencia local: build de `ssa_integration_tests`; CTest causal `3/3` em
  `0.36 s`; workflow `174/174` em `7.38 s`; repeticao `10x` do journal em
  `0.78 s`; gates estaticos limpos. Bitbucket confirma `6bcf65e`; GitLab
  `origin` segue OAuth `invalid_grant`.
- `clawpatch` local nao iniciou review por configuracao/modelo externo
  incompativel; nao e validacao concluida. Nao houve mudanca de producao,
  schema, regras de recovery ou credito nos contadores.

Proxima atividade unica: priorizar a proxima pendencia local de confiabilidade
de importacao/SQLite fora do denominador fixo.

## Cancelamento causal do conversor legacy - 2026-07-18

- `0172f2e` troca o loop que aguardava `.conversion-ready` por um runner falso
  injetado pelo port ja existente. Ele cria output parcial, sinaliza
  `binary_semaphore` e aguarda `stop_token` por `stop_callback`; o teste cancela
  somente depois do checkpoint observado.
- Nao houve mudanca de producao: o corte prova o boundary do conversor e seu
  cleanup, enquanto spawn e kill de filho OS continuam responsabilidade dos
  contratos de `SupervisedProcess`. Ramo fake e include sem uso foram removidos.
- Evidencia local: gates estaticos limpos, `ssa_integration_tests` compilado,
  familia legacy `4/4` em `0.08 s` e workflow `174/174` em `6.85 s`.
  `clang-tidy` filtrado nao achou aviso novo; review Terra ultra final:
  `SEM FINDINGS`. Bitbucket confirma `0172f2e`; GitLab `origin` segue OAuth
  `invalid_grant`.
- Contadores nao mudam: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila operacional `5/8 = 62.5%`.

Proxima atividade unica: converter os checkpoints polling dos crash probes
SQLite em sinais causais, sem mudar semantica de crash ou recovery.

---

## Checkpoint causal de copia/staging - 2026-07-18

- `79f8200` fecha os sete polls diretos de arquivo temporario no staging. A
  copia atomica aceita um callback opcional apos a primeira escrita e o workflow
  o encaminha ao stager; no caminho normal o callback esta vazio e nao altera
  schema, GUI, regras de importacao ou timing de producao.
- O teste novo foi escrito antes da correcao: `1/1` falhou porque uma excecao do
  callback escapava. Review Terra ultra achou P2 no cleanup e outro revisor o
  confirmou. A implementacao fecha e tenta remover o `.part`, devolvendo
  `FileCopyStatus::Failed` ou `CleanupFailed` com diagnostico. Review final:
  `SEM FINDINGS`.
- Evidencia local: gate estatico limpo (Semgrep `11` regras, zero finding),
  `ssa_integration_tests` compilado, contratos diretos `8/8` em `0.80 s` e
  workflow `174/174` em `8.65 s`. `clang-tidy` so repetiu avisos preexistentes
  no workflow; hooks do commit passaram formatacao e scanners staged.
- Publicacao: Bitbucket confirma `79f8200`; GitLab `origin` permanece
  indisponivel por OAuth `invalid_grant`. Contadores nao mudam: plano original
  `99.0/100`, divida nova `13/14 = 92.9%`, legado `0.0/100` placeholder e fila
  operacional `5/8 = 62.5%`.

Proxima atividade unica: diagnosticar o polling de processo do conversor legacy
e introduzir somente o handshake causal necessario para seus contratos.

---

## Polling de staging e corpus lock removido - 2026-07-18

- `1f99a90` altera somente tres contratos de integracao. `writerBusyEntered`
  substitui loops de staging e um wait fixo de `50 ms`, provando que staging e
  a primeira contencao SQLite ocorreram antes de cancelar ou testar o lock.
- O teste de cleanup passa a criar bloqueio SQLite real antes de trocar a
  permissao; o teste de corpus lock mantem o `QLockFile` concorrente como prova
  do contrato. Nao ha mudanca de schema, API publica ou comportamento normal.
- O review Terra ultra encontrou P1 no primeiro diff: `snapshotLocked` pertence
  a publication, mas este rescan bloqueia no preflight. A troca para
  `writerBusyEntered` corrigiu a causalidade; review final `SEM FINDINGS`.
- Evidencia local: build afetado, CTest focado `3/3` em `0.48 s` e workflow
  completo `174/174` em `7.72 s`; gate estatico limpo. Bitbucket confirma
  `1f99a90`; GitLab `origin` permanece OAuth `invalid_grant`.
- Percentuais sem credito novo: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: atacar o proximo polling de staging apenas depois de
definir evento causal de copia ou conversao.

## Sinais causais de contencao SQLite na importacao - 2026-07-18

- Codigo entregue em `f62ea53` no branch `master`; Bitbucket confirma o hash.
  GitLab `origin` continua bloqueado por OAuth `invalid_grant`; nao conta como
  validacao externa concluida.
- O writer recebe sinal opcional de primeira callback busy e a sessao retem seu
  ownership. O workflow separa evento do writer/journal (`writerBusyEntered`) de
  contencao no Backup API de publication (`snapshotLocked`).
- Os testes removem precondicoes de `50/250/500 ms`, aguardam o evento causal e
  preservam watchdogs de cancelamento e cleanup. P2 de permit anterior no
  cleanup e P2 de dupla liberacao `BUSY/LOCKED` foram corrigidos; review final
  Terra ultra: `SEM FINDINGS`.
- Evidencia local: build afetado; CTest causal `7/7` em `0.56 s`; workflow
  `174/174` em `7.96 s`; familia SQLite Repository `31/31` em `0.72 s`.
  Diff, formato, cppcheck, detect-secrets e Semgrep limpos. Hooks de commit
  passaram Gitleaks e TruffleHog.
- Percentuais sem credito novo: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: remover polling de staging e corpus lock ainda presente
nos testes de importacao, somente quando houver evento causal equivalente.

## Benchmark da formatacao de pagina - 2026-07-18

- `ce90132` entrega o target `ssa_table_page_format_benchmark` e seu smoke CTest.
  A fixture preconstruida usa `500x12` valores reais de texto, inteiro e data;
  portanto mede somente o custo do formatter, nao I/O, query SQLite ou paint QML.
- Medicao final: 30 amostras de 10 iteracoes, wall p50/p95
  `2.93215/3.3340834 ms` e CPU p50/p95 `2.9244/3.2649 ms` por pagina. O JSON
  local nao versionado fica em `build/dev/table-page-format-benchmark-30.json`.
- Decisao: manter cache eager. O formatter ja executa no worker e a medicao nao
  justifica mudar `SsaTableDisplayValues`, cache ou layout por lazy formatting.
- Validacao: gate estatico limpo, target afetado verde, smoke `1/1` em `0.04 s`
  e review Terra ultra `SEM FINDINGS`. Bitbucket confirma `ce90132`; GitLab
  `origin` permanece OAuth `invalid_grant`, com local 74 commits a frente.
- Percentuais inalterados: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: substituir precondicoes temporais de `sqliteBusyWait`
nos contratos de importacao/rescan por sinais causais da primeira contencao SQLite.

## Reconciliacao do macro report - 2026-07-18

- P4 estava stale: o HEAD ja delega o macro report para analytics SQL com
  `COUNT(DISTINCT "ssa_number")`; o repositorio so materializa as linhas e nao
  existe `map<ReportKey,set<string>>` para migrar.
- Tres contratos locais passaram `3/3` em `0.13 s`, cobrindo SQL, filtros e
  agrupamento. Nao ha benchmark novo porque o gargalo descrito foi refutado.
- Percentuais inalterados: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: medir a formatacao eager da tabela antes de qualquer
mudanca de cache ou GUI.

## Determinismo causal do repositorio SQLite - 2026-07-18

- `28700b3` troca polling de lock e waits de `10/50 ms` por sinais do lock
  adquirido, busy handler e progress handler SQLite. A seam opcional tem
  ownership explicito por `shared_ptr<counting_semaphore>` e fica nula em
  producao.
- Review encontrou P2 de lifetime/overflow e P1 de tipo entre handler e
  repositorio; ambos foram corrigidos antes do review final Terra ultra
  `SEM FINDINGS`.
- Evidencia local: gate estatico limpo, build afetado verde e familia SQLite
  Repository `31/31` em `0.68 s`; hooks staged de formatacao e segredos verdes.
- Bitbucket confirma `28700b3`; GitLab `origin` permanece OAuth `invalid_grant`;
  local `71` commits a frente. Percentuais inalterados: plano original
  `99.0/100`, divida nova `13/14 = 92.9%`, legado `0.0/100` placeholder e fila
  operacional `5/8 = 62.5%`.

Proxima atividade unica: medir e provar a equivalencia SQL do macro report com
`COUNT(DISTINCT)` antes de alterar a implementacao.

## Indice status-last de producao - 2026-07-18

- `4396e1c` conecta o indice de expressao DESC ao writer e reutiliza a mesma
  expressao SQL do query builder. A criacao ocorre somente para tabelas com
  `numero_ssa` e `situacao`; um DB ja existente e atualizado na proxima escrita,
  sem bump de schema ou trabalho em leitura.
- Evidencia: contratos para DDL/plano exato, replace apos remocao, tabela legacy
  customizada e colisao de nome fail-closed. O primeiro review Terra ultra
  encontrou P2 de ownership e cobertura legacy, corrigidos antes do review final
  `SEM FINDINGS`.
- Validacao local: gates estaticos limpos (Semgrep `62` regras, zero finding),
  build afetado passou e CTest direto `38/38` em `1.14 s`.
- Publicacao: Bitbucket confirma `4396e1c`; GitLab `origin` permanece OAuth
  `invalid_grant`; local `69` commits a frente. Contadores permanecem plano
  original `99.0/100`, divida nova `13/14 = 92.9%`, legado `0.0/100` placeholder
  e fila operacional `5/8 = 62.5%`.

Proxima atividade unica: eliminar polling temporal de `SqliteRepositoryTests`
por sinais causais de lock, busy e progress.

## Benchmark SQLite status-last - 2026-07-18

- `1d950e3` entrega o harness isolado com fixture de `250000` linhas e `30`
  amostras; o JSON local nao versionado fica em
  `build/dev/sqlite-status-last-benchmark-30.json`.
- O plano baseline registra `TEMP B-TREE`. A variante indexada usa
  `idx_ssa_table_status_last_numero_ssa_desc`, nao registra `TEMP B-TREE` e
  preserva vetores de resultados identicos.
- Wall p50/p95: baseline `41.260958/45.094667 ms`, indexado
  `0.0235/0.030125 ms`. CPU p50/p95: baseline `41.184/44.742 ms`, indexado
  `0.024/0.031 ms`.
- Gates estaticos limpos; smokes `2/2` em `0.37 s`; Terra ultra final
  `SEM FINDINGS`. Escopo limitado a fixture SQL: sem indice de producao e sem
  prova de filtros arbitrarios ou GUI completa.
- Publicacao historica: Bitbucket `0/0` em `1d950e3`; GitLab `origin` bloqueado
  por OAuth `invalid_grant`; local `67` commits a frente de `origin/master`.
- Contadores inalterados: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado placeholder `0.0/100`, fila operacional `5/8 = 62.5%`.

Proxima atividade unica: RED de integracao e indice DESC de expressao no writer.

---

## Cobertura sintetica de URL Windows/UNC - 2026-07-18

- HEAD anterior: `e76e722`. O commit `bca795a` entrega somente em
  `AdvancedPopupQmlTest.cpp` um contrato para `file:///C:/SSA/graph.png` e
  `file://server/share/graph.png`: `DerivadasGraphModel::localFilePath()`
  delega exatamente para `QUrl::toLocalFile()`. Na publicacao de `bca795a`,
  Bitbucket foi confirmado com divergencia `0/0`; GitLab `origin` falhou por
  OAuth `invalid_grant`, e o HEAD local estava 65 commits a frente de
  `origin/master`.
- Validacao local: `ssa_qml_advanced_popup_tests` `1/1` em `2.16 s`;
  `git diff --check`, clang-format e detect-secrets passaram; Semgrep executou
  53 regras com zero finding. Review Terra ultra terminou sem findings.
- Sem producao e sem credito: plano original `99.0/100`; divida nova
  `13/14 = 92.9%`; backlog legado `0.0/100` como placeholder; fila operacional
  `5/8 = 62.5%`.
- Risco residual: cobertura sintetica nao prova SMB, `CreateFileW`, reparse
  points, handles de diretorio ou gravacao em share. Proxima atividade unica:
  prova Windows/UNC real para importacao e grafo. Profiling do prefetch continua
  bloqueado.

---

## Fechamento local de tooling - 2026-07-18

Status: `ENTREGUE` localmente e no Bitbucket. GitLab permanece parcial por
OAuth `invalid_grant`.

- Branch `master`; HEAD de codigo `2e2476b`, Bitbucket `0/0`; local 63 commits
  a frente de `origin/master`.
- Contadores: plano original `99.0/100`; divida nova `13/14 = 92.9%`; backlog
  legado `0.0/100` como placeholder; fila operacional `5/8 = 62.5%`.
- `CMAKE_OSX_SYSROOT=macosx` esta nos cinco presets-raiz; `dev-arrow` herda
  `dev`. O compile database registra SDK macOS 26.5. `clang-tidy` direto passou
  em `2.50 s`; build `ssa_infra` 58 passos em `5.55 s`, integracao 160 passos
  em `13.19 s` e mapper `15/15` em `0.44 s`.
- Semgrep 1.170 focado no teste de importacao de 6,815 linhas passou em `2.92
  s`, com duas regras atuais elegiveis, zero finding e zero timeout. A alegacao
  `SEMGREP-WIDE-NARROW-TIMEOUT` de `63554b9` nao tem IDs, YAML, fixture ou log;
  nenhuma fixture foi dividida. A equivalencia exata ao pin 1.169 do CI nao foi
  validada: socket Docker local ausente e dependencia externa.

Proxima atividade unica: prova Windows/UNC em plataforma real para importacao e
grafo. Profiling do prefetch continua bloqueado; handles, PowerShell e
packaging seguem externos.

---

## Fechamento de determinismo e filename - 2026-07-18

Status: `ENTREGUE` localmente e no Bitbucket. GitLab permanece parcial por
OAuth `invalid_grant`.

- Branch `master`; HEAD de codigo `be20b99`, Bitbucket `0/0`. Antes do commit
  documental, o local estava 61 commits a frente de `origin/master`.
- Contadores: plano original `99.0/100`; divida nova `13/14 = 92.9%`; backlog
  legado `0.0/100` como placeholder sem denominador; fila operacional
  `4/8 = 50.0%`.
- `TEST-DETERMINISM-DELTA` foi fechado pelos commits de `785c73e` ate
  `bcb7980`. A matriz focada passou `26/26` em `28.38 s`; o mapper passou
  `30/30` em `2.21 s`, preservando sua janela temporal deliberada. Os waits
  residuais observam filesystem, processo, SQLite, timer ou cancelamento real;
  nenhum timeout foi tratado como sucesso.
- `IMPORT-FILENAME-TIMESTAMP-SCAN` foi fechado em `be20b99`: target e build
  passaram, o gate passou `3/3` em `0.41 s` e o recheck `3/3` em `0.13 s`.
  Foram 30 processos, 12,000 parses e `2.00 s`; p95 ISO `783.3 ns` e maximo
  adversarial `178269.15 ns`, ou `0.178 ms`. O parser e O(n), sem justificativa
  para otimizacao.
- O CTest sequencial passou `595/595` em `66.86 s`. O novo smoke foi registrado
  depois e elevou a matriz configurada para 596; logo, esse full nao inclui o
  teste 596.
- Security ampla: Semgrep `509` arquivos, `24` regras, zero finding; Gitleaks
  `1.42 GB`, zero leak; TruffleHog `9,226` chunks, zero segredo.
- Resta no denominador fixo apenas `IMPORT-STAGER-HUB`, dependente de
  Windows/UNC real. No plano original, resta 1.0 de profiling do prefetch,
  bloqueado pela ferramenta local. Tambem restam tooling local e provas reais
  de handles, PowerShell e packaging.

Proxima atividade unica: canonizar clang-tidy macOS com sysroot e reproduzir
os IDs Semgrep. O profiling segue bloqueado; depois executar as provas externas.

---

## Historico anterior: fechamento de estabilizacao - 2026-07-18

Status: `ENTREGUE` localmente e no Bitbucket. GitLab permanece parcial por
OAuth `invalid_grant`.

- Branch `master`; HEAD de codigo `b2369ac`, precedido por `7291082`, `0025d44`
  e `7f396b0`.
- Plano original: `99.0/100`; profiling valido do prefetch continua sendo o
  unico ponto original sem aceite.
- Divida nova: `78.6/100`, 11 de 14 itens. O credito novo pertence a
  `SQLITE-READ-CONNECTION-CHURN`; findings posteriores a lista fixa nao reduzem
  retroativamente o percentual.
- Backlog legado: `0.0/100`, sem denominador aceito.
- Fila prioritaria operacional: `2/8 = 25.0%`. Busy wait integral e gate de
  schema estao resolvidos. Restam determinismo, filename benchmark, profiling,
  tooling e dois pacotes de prova multiplataforma.
- Os tres itens que seguram a divida nova sao `IMPORT-STAGER-HUB`,
  `IMPORT-FILENAME-TIMESTAMP-SCAN` e `TEST-DETERMINISM-DELTA`. Os dois locais
  elevam o contador para `13/14 = 92.9%`; o ultimo exige Windows/UNC real.
- Activity Analytics passou a validar e reparar metadata/storage de forma
  deterministica e fail-closed. O gate focado passou `79/79`.
- Importacao aceita chunks 1-1000 e busy wait SQLite 0-3000 ms em passos de
  5 ms; defaults sao 1000 e 3000 ms. Validacao ocorre antes de staging/lock e
  propagacao cobre full, incremental, external e recovery.
- `7291082` fecha a lacuna concreta de `sqliteBusyWait`: o Backup API ja
  aguardava contencao temporaria, mas recovery lookup e cleanup do journal
  mantinham 250 ms fixos. O request agora governa todas essas fases; defaults
  diretos e cancelamento ja solicitado preservam o limite anterior.
- `b2369ac` persiste `schemaVersion()==1` na write transaction, aceita legado
  zero e rejeita versoes futuras no writer, recovery lookup, journal cleanup e
  validator. Recovery falha antes de mover fontes e o validator usa um unico
  snapshot de leitura para versao, estrutura e registros.
- O RED moveu uma fonte sob schema futuro. Depois do patch, contratos
  principais passaram `80/80`, cleanup direto passou `30/30` e o gate amplo
  passou `45/45` em `2.87 s`. A primeira matriz terminou `590/594`; os quatro
  casos passaram isolados e `40/40` repeticoes. A matriz final passou `595/595`
  em `75.81 s`.
- Benchmark de 250 mil linhas concluiu 30 amostras por cenario em primeira
  passagem e reabertura idempotente. Legacy first-pass teve wall p50/p95
  `1959.458/3258.614 ms`; reabertura ficou abaixo de `3.894 ms` no p95.
- Benchmark do repository real concluiu 30 processos e 3,000 reads repetidos:
  first read wall p50/p95 `6.948/12.897 ms`; open/read/close
  `1.390/3.296 ms`. As 2,400 chamadas concorrentes de `page()` terminaram sem
  falha, com latencia agregada p50/p95 `157.501/219.458 ms`. O cache do SO nao
  foi controlado; pool nao foi justificado.
- Em `4e74790`, build dev completo passou, CTest sequencial passou `581/581` em
  `82.06 s` e Semgrep, Gitleaks e TruffleHog amplos ficaram limpos.
- `2dd9aec` substitui `QSaveFile` por SQLite Backup API no destino. Os 3 casos
  inicialmente RED passaram em `0.22 s`; o gate ampliado passou `7/7` em
  `0.35 s`. O Sol encontrou e fechou 1 P1 na corrida entre `SQLITE_DONE` e stop
  tardio; re-review sem P0-P2.
- `EXPLAIN QUERY PLAN` confirmou que `COLLATE NOCASE` impede seek no indice
  BINARY de `numero_ssa`: `43/71 ms` e 94,879 linhas visitadas viram `2/<1 ms`
  com comparacao BINARY. Nenhum pool ou schema novo e necessario.
- `1cea684` aplica BINARY apenas a SSA canonica. Os 3 casos passaram em
  `0.12 s`, o gate correlato passou `45/45` em `2.02 s` e o benchmark terminou
  2,400/2,400 reads sem falha. `page()` p50/p95 ficou em `0.133/0.229 ms`,
  contra `157.501/219.458 ms` na baseline.
- `360d18b` move QLockFile, open, handlers, transacao, DDL/DML e log para fora
  de `derivedCountSummaryMutex_`. Um unico initializer e eleito; concorrentes
  usam fallback e cancelamento/excecao libera retry. O RED falhou em `1.06 s`;
  GREEN passou 2/2, 60 repeticoes, `13/13` e `8/8`. Sol e Terra fecharam 1 P2
  de cobertura e deram GO no re-review.
- Os oito testes informados como falhos no registro antigo de 564 casos
  passaram `8/8` em `10.09 s` no registro atual. A matriz agora tem 588 casos.
- Na verificacao mais recente, os mesmos oito nomes passaram `8/8` em `9.77 s`.
  Os tres contratos de busy wait passaram 30 execucoes e o gate ampliado de
  journal, cancelamento, WAL, SAM e rescan passou `15/15` em `2.67 s`.
- `610fbf3` fecha o recheck WAL: leitor em transacao preserva o snapshot antigo
  durante a publicacao e observa o novo na transacao seguinte.
- `7f396b0` estabiliza o harness temporal sem alterar codigo de producao. O
  conversor passou 20/20 repeticoes, SAM 20/20 suites, Details 10/10 e
  CurrentWeek 20/20. O CTest final passou `588/588` em `78.30 s`.
- Security ampla atual: Semgrep 508 arquivos e 24 regras, Gitleaks 1.42 GB e
  TruffleHog Git 9,090 chunks, todos sem finding. Os cinco arquivos alterados
  tambem passaram TruffleHog sem warning.
- Sol encontrou e fechou 1 P1 de recovery, 1 P2 de snapshot e 1 P2 de
  cobertura do cleanup. O recheck final apos o ultimo teste excedeu a janela e
  nao recebeu credito. Clawpatch permaneceu invalido por CLI antigo.
- Bitbucket `master` foi confirmado em `b2369ac`; Windows/UNC, PowerShell e
  packaging seguem prova externa. GitLab continua bloqueado por `invalid_grant`.

Proxima atividade unica: fechar `TEST-DETERMINISM-DELTA` por suite e depois
executar o benchmark formal de timestamp em filename. Somente depois retomar o
profiling, sem atribuir credito a trace invalido.

---

## Historico superado: marco P0 local

Este bloco registra o estado daquele marco e foi substituido pelo fechamento no
topo. Naquele momento, o status era `ENTREGUE` no working tree e commitado
localmente. Bitbucket estava comprovado em `55c6bd8`; GitLab `origin` estava
pendente por OAuth `invalid_grant`.

- Branch `master`; HEAD operacional inclui os slices commitados desta rodada;
  o hash atual deve ser lido pelos refs remotos no fechamento.
- Plano original: `89.0/100 -> 99.0/100`.
- Divida nova: `0.0/100 -> 64.3/100`, por 9 dos 14 itens enumerados aceitos.
- Backlog legado: nenhum credito atribuido; denominador ainda nao definido.
- Slice 1 entregue: Different canonico `!=valor`, leitura legada de `!valor`,
  busca geral preservada e distinct com advanced text filters remanescentes.
- Slice 2 entregue: estado compartilhado no log, I/O fora do mutex global,
  drain de handlers em voo e truncamento UTF-8 valido.
- Restante original, menus: 5/5 fluxos usam pointer real e efeito terminal.
  Nenhum QML de producao ou layout precisou mudar; credito `5 x 0.78 = 3.9`.
- Restante original, popup: smokes 1180/1580 passaram 2/2 e as imagens foram
  reconciliadas sem defeito visual; credito `0.8`, sem edicao de layout/QML.
- Restante original, grafo: geometria unica e URL local tipada/fail-closed
  passaram 2/2 suites e qmllint; credito `0.8 + 0.8 = 1.6`. Windows/UNC e
  evidencia externa pendente.
- Restante original, prefetch: harness isolado, 30 amostras e relatorio
  quantitativo passaram o target e o smoke focado; credito
  `1.6 + 1.2 + 0.9 = 3.7`. Profiling valido permanece pendente por `1.0`.
- Fronteira pequena, widths: as 85 larguras default agora sao pareadas por key
  no catalogo de presentation; valores preservados e teste focado aprovado.
- Fronteira pequena, request coordinator: o cast concreto foi removido em favor
  da API publica de exclusao; o teste focado de filtros passou.
- Fronteira pequena, completeness: `completenessScore()` usa o valor ja
  iterado, eliminando lookup redundante; os dois casos de richness passaram.
- Fronteira pequena, SQL de derivadas: a consulta foi movida ao query builder
  com binding unico; testes unitario e de cancelamento passaram.
- Fronteira pequena, filtros avancados: o factory usa diretamente as keys
  canonicas, removendo lookup e null branch redundantes; suite focada passou.
- Fronteira pequena, column filters: o view model usa diretamente as keys
  ordenadas canonicas, removendo lookup e null branch redundantes; suite passou.
- Test debt, quick sector: o caso sincrono removeu `QTRY_COMPARE` e timeout
  arbitrario; o caso focado passou 1/1. Sem credito adicional no denominador.
- Test debt, quick sector adicional: o segundo caso sincrono tambem usa
  `QCOMPARE`; o resumo `activeFilterEntries()` manteve `QTRY` por depender do
  timer de 120 ms. A suite de painel passou `1/1` em `2.96 s`.
- Test debt, text filter: dois casos sincronicos apos `setQuickSector()` usam
  `QCOMPARE`; a suite passou `1/1` em `5.74 s`, sem credito adicional no item
  binario amplo.
- Test debt, sector refresh: um caso sincronico apos `setTextFilter()` usa
  `QCOMPARE`; a suite passou `1/1` em `2.88 s`. Resumos com timer permanecem.
- Performance, roleNames: cinco implementacoes foram localizadas sem hot path
  de producao comprovado; cache foi deferido e nenhum credito foi atribuido.
- Importacao, filename timestamp: caso adversarial com ruido numerico de 4096
  caracteres passou `1/1` em `0.02 s`; parser preservado e credito de performance
  ainda pendente.
- Gate amplo posterior: CTest sequencial confirmou `453/453` em `64.05 s`;
  nenhum timeout foi tratado como sucesso.
- `IMPORT-WORKFLOW-HUB` foi fechado nos commits `14485a9` e `627a075`:
  adaptacao SAM e importacao XLSX por chunks usam resultados internos tipados,
  enquanto a mesma sessao, catches, rollback, journal e consolidacao permanecem
  no orquestrador. O teste de falha tardia preserva contadores e prova rollback.
- `IMPORT-STAGER-HUB` foi implementado localmente em `55c6bd8`:
  `ImportFileStager.cpp` caiu de 1364 para 634 linhas, e consolidacao passou a
  ter ownership concreto em `ImportFileConsolidator`. Gate focado `11/11` e
  suite completa `453/453` passaram; Windows/UNC real continua pendente e nao
  recebeu credito binario.
- Determinismo, database switch: tentativa de trocar dois loops por `QTRY`
  falhou no gate amplo por alterar a janela preterminal; `9b960ed` restaurou o
  polling deliberado e a suite passou `1/1` em `0.75 s`, sem credito.
- Reteste do gate: database switch passou novamente e os testes de RSS/memoria
  `449-452` passaram `4/4` em `12.24 s`.
- Cleanup residual, conversor XLS: o diagnostico de output ausente deixou de
  ficar vazio; o build de integracao e os quatro casos de conversao passaram.
- Review acumulado: 3 Important e 4 Minor. Os 3 Important e 3 Minor foram
  corrigidos; permanece 1 Minor aceito de perda de linha na janela exata de
  uninstall, sem UAF, race ou deadlock.
- Primeiro CTest integral: 447/449 em 75.49 s por 6 expectativas smoke antigas.
- CTest integral final: 449/449 em 63.68 s.
- Build dev completo: PASS. Semgrep amplo, Gitleaks e TruffleHog: zero finding
  ou segredo.
- `.qmltypes` esta gerado e `all_qmllint` passou. Handles TOCTOU ja existem
  localmente; falta validacao externa Windows. Importacao de derivadas ja esta
  presente; filtros genericos de semana/derivacao permanecem ocultos.

Impacto de arquivos do marco:

- Query/domain: contrato canonico e aplicacao de filtros advanced em distinct.
- Desktop/platform: lifetime do handler e truncamento de log.
- Testes/CMake: o target QtTest do log elevou a matriz para 449 e o smoke do
  benchmark para 450. O full anterior passou 449/449; o smoke novo passou 1/1
  separadamente e ainda nao existe execucao integral 450/450.
- Documentos: status, backlog e este handoff reconciliados.

Proxima atividade naquele marco: profiling valido do prefetch.

---

## 1. Baseline historico anterior ao marco P0

Esta secao registra o ponto de partida e foi substituida operacionalmente pela
atualizacao de execucao no topo. As afirmacoes abaixo sobre ausencia de mudanca
C++/CMake/testes descrevem somente o inicio da rodada.

- Branch: `master`.
- HEAD publicado: `d376431372648826d3ee9e1182ea5785a0d31aa0`.
- Tag: `v0.9.10`, anotada e confirmada em `origin` e `bitbucket`.
- Suite reexecutada no HEAD: 443/443 em 72.09 segundos.
- Build: `cmake --build --preset dev -j 6`, sem trabalho pendente.
- Nenhum C++, QML, CMake ou teste foi alterado durante a reauditoria.
- Working tree documental esperado no inicio do handoff:
  - `.gitignore` modificado para ignorar `backups/`;
  - `ROUND_STATUS.md` modificado;
  - `RECOVERY_BACKLOG.md` modificado;
  - este handoff novo.
- O usuario moveu backups locais para `backups/`. Nao restaurar nem apagar os
  quatro deletes tracked da raiz sem comando explicito. `backups/` deve ficar
  ignorado.
- Outros untracked locais continuam fora do Git: `.safety-project.ini`,
  `.serena/`, documentos de paridade e `docs/audits/`.
- Nao criar branch, worktree, PR, merge ou publicar em GitHub sem autorizacao
  dedicada.

## 2. O que os reports acertaram

### Report A: documento tecnico consolidado

O report identificado pelo inicio `Tudo confirmado` acertou os seguintes
pontos:

| Finding | Veredito | Evidencia atual |
| --- | --- | --- |
| D1, workflow de importacao | Confirmado | `SpreadsheetImportWorkflowPort.cpp` tem 1285 linhas e 23 catches; `importDiscoveredFiles()` tem 454 linhas e 11 catches. |
| A2, lookup redundante | Resolvido no working tree | `completenessScore()` usa diretamente o `value` iterado; richness e metadata permanecem equivalentes. |
| A4, timestamp de filename | Confirmado como candidato de medicao | O parser tenta cada sufixo iniciado por digito; pior caso quadratico, limitado pelo tamanho do filename. |
| D4, conexao por read | Confirmado como fato | `SqliteSsaRepository` abre conexao e handlers por operacao. Pode custar latencia, mas tambem isola threads/cancelamento. |
| D5, SQL de derivadas | Resolvido no working tree | `derivadasDiretas()` usa `buildDirectDerivations()` com binding unico e SQL preservado. |
| D7, cast sem check | Resolvido no working tree | `BrowseRequestCoordinator` usa `excludeScaSesSte()` diretamente e nao depende mais de cast concreto. |
| M1, widths posicionais | Resolvido no working tree | As 85 larguras sao pares `{key, width}` e o lookup nao depende da ordem do domain. |
| M2 e M3, waits em SQLite | Confirmados como test debt | Existem `sleep_for(1050ms)` e polling de 2ms com deadline. |
| M4, screenshot sem diagnostico | Confirmado | `captureDialogScreenshot()` retorna apenas false em timeout/falha. |
| N2, UTF-8 truncado | Confirmado | `RotatingLogWriter` corta `std::string` por bytes antes do sufixo. |
| N4 e N5, custo/cobertura do log | Confirmados | O arquivo e aberto por linha e nao existe teste explicito de payload multibyte maior que o cap. |

O report tambem acertou ao corrigir dois erros da auditoria anterior:

- A6-1 foi refutado corretamente: `defaultWidth == 98` nao esta em
  `DomainTests.cpp`.
- B22-4 foi refutado corretamente: 1769 XLSX representa corpus de inventory,
  nao o limite da selecao externa.

### Report B: reauditoria delta pos-fix

O report identificado por `Reauditoria delta pos-fix` adicionou tres findings
prioritarios que nao estavam destacados com clareza no plano anterior:

| Finding | Veredito | Impacto |
| --- | --- | --- |
| `!valor` compila como not-contains | Confirmado High | A GUI chama o operador de Different, mas o SQL remove qualquer substring. `ADM` e `PRE-ADM` produzem comportamento diferente do esperado. |
| Distinct omite advanced text filters | Confirmado High | As opcoes de popup podem ser calculadas sem parte dos filtros ativos. |
| DesktopLogSink faz I/O sob mutex | Confirmado High | Todos os produtores Qt podem ficar serializados pela latencia do filesystem; a secao critica inclui rotacao e append. |

O mesmo report acertou ao manter como riscos ou decisoes pendentes:

- conflito entre snapshot filename-first e outras politicas Python;
- enrich de campos em estado terminal versus freeze Python;
- normalizacao integral da tabela existente;
- necessidade de fixture SAM externa real;
- divergencias de parsing que dependem de decisao de produto;
- hubs de importacao ainda grandes apesar do hardening funcional.

## 3. O que os reports adicionaram ao backlog

Novos itens validos incorporados aos documentos de controle:

1. `FILTER-DIFFERENT-SEMANTICS`.
2. `DISTINCT-ADVANCED-TEXT-FILTERS`.
3. `DESKTOP-LOG-HANDLER-IO-LOCK`.
4. `IMPORT-WORKFLOW-HUB`.
5. `IMPORT-STAGER-HUB`.
6. `COLUMN-WIDTH-KEY-PAIRING`.
7. `ROTATING-LOG-UTF8-TRUNCATION`.
8. `SQLITE-IMPORT-NORMALIZE-FULL-SCAN`.
9. `SQLITE-READ-CONNECTION-CHURN`, condicionado a benchmark.
10. `SQL-DERIVADAS-BUILDER-BOUNDARY`.
11. `BROWSE-SECTOR-CAST-INVARIANT`.
12. `IMPORT-COMPLETENESS-LOOKUP`.
13. `IMPORT-FILENAME-TIMESTAMP-SCAN`, condicionado a benchmark.
14. `TEST-DETERMINISM-DELTA`.

## 4. O que os reports erraram ou superestimaram

| Alegacao | Classificacao final | Razao |
| --- | --- | --- |
| C1 e C3 sao arvores condicionais profundas/lentas | Superestimado | Sao cadeias flat de dispatch. A contagem de branches nao demonstra gargalo e uma strategy table pode piorar legibilidade. |
| D6 exige outro port para WorkflowCommandRunner | Refutado | Presentation depender do facade application `SsaWorkflowService` respeita a direcao atual. Outro port duplicaria contrato sem consumidor independente. |
| RotatingLogWriter ignora erro de rename | Refutado | A overload usada por `std::filesystem::rename` lanca em falha. O retorno bool de remove somente distingue inexistencia. |
| Watchers principais continuam com payload Qt nao-trivial | Obsoleto | Os fluxos principais foram migrados para `QFutureWatcher<void>` com estado sincronizado apos o fix TSan. |
| A2 e incidente High | Severidade excessiva | O lookup e redundante, mas o numero de colunas e limitado. E quick win de baixo risco, nao falha operacional. |
| Conexao SQLite por read e bug High | Nao comprovado | Pode ter custo, mas preserva isolamento por thread e cancelamento. Exige benchmark antes de pool/conexao longeva. |
| SQL de derivadas e vulneravel por concatenacao | Nao demonstrado | Os nomes atuais sao literais canonicos e table name validado. O problema e de ownership da query, nao injection provada. |
| `-valor` precisa virar negacao | Decisao de produto | O C++ preserva hifen literal por teste. Alterar automaticamente quebraria busca de conteudo com hifen. |
| `~` deve seguir regex Python | Decisao de seguranca/produto | O C++ usa SafePattern restrito intencionalmente. Regex ampla nao entra sem contrato explicito. |
| Estado operacional em `d34f92d`, 439 testes | Obsoleto | Naquele fechamento, o estado avancou para `d376431`, tag `v0.9.10`, e 443/443 testes verdes. O topo deste handoff e a fonte operacional atual. |

## 5. Lacunas que os reports nao resolveram

- Nenhum report definiu o contrato de self-filter para distinct values. Antes
  do fix, decidir: filtros de outras colunas sempre entram; o filtro da coluna
  alvo precisa de teste que prove a UX desejada.
- O report de log recomenda retirar o mutex, mas nao resolve lifetime do ponteiro
  static `active_`. Copiar o raw pointer e liberar o lock cria use-after-free.
  O desenho precisa ownership estavel ou fila limitada com shutdown.
- A recomendacao de dividir D1 em quatro colaboradores foi generica. O primeiro
  corte seguro e uma operacao interna de workbook/chunks com resultado tipado;
  depois, se ainda houver coesao ruim, separar publicacao/consolidacao.
- Os reports contam sleeps e `/tmp`, mas nao distinguem path apenas simulado de
  I/O real. A migracao deve ocorrer por suite, sem alterar delays usados para
  testar cancelamento deliberado.
- Nenhum report fecha pointer real de menus, grafo multiplataforma, tooling
  incompleto ou TOCTOU por handle. Esses itens antigos continuam depois dos
  novos findings.

## 6. Ordem de execucao recomendada

### Slice 1: Semantica de filtros e distinct values

**Files:**

- Modify: `src/domain/TextFilterToken.cpp`
- Modify: `src/query/SqlQueryBuilder.cpp`
- Test: `tests/unit/TextFilterTokenTests.cpp`
- Test: `tests/unit/SearchParserTests.cpp`
- Test: `tests/unit/SqlQueryBuilderTests.cpp`
- Test: `tests/integration/SqliteRepositoryTests.cpp`

#### Step 1: Criar o RED de Different exato

Adicionar registros `ADM`, `PRE-ADM` e `APV`. Aplicar o token produzido pelo
modo Different para `ADM`.

Expected: o teste atual falha porque `PRE-ADM` tambem e removido.

#### Step 2: Preservar a negacao Contains geral

Adicionar caso separado para busca geral `!ADM` e provar que o contrato atual
continua sendo not-contains.

#### Step 3: Corrigir a fronteira do token tipado

Fazer o token de `TextFilterOperator::Different` produzir a forma exata aceita
pelo parser, sem mudar a sintaxe da busca geral.

#### Step 4: Criar o RED de distinct com advanced filters

Usar filtro avancado em outra coluna e provar que o conjunto distinct muda.
Adicionar caso explicito para o filtro da coluna alvo antes de decidir se ele e
mantido ou removido.

#### Step 5: Implementar o minimo no query builder

Reusar `appendAdvancedTextFilters`; nao duplicar parsing nem mover SQL para
presentation.

#### Step 6: Executar testes focados

Run:

```bash
ctest --preset dev --output-on-failure -R "search parser|text filter token|sql query builder|sqlite repository"
```

Expected: PASS.

#### Step 7: Gate e commit atomico

Executar review C++ aplicavel, build e CTest focado. Commit somente mediante
autorizacao explicita do usuario.

### Slice 2: Logging concorrente e UTF-8

**Files:**

- Modify: `app/desktop/DesktopLogSink.h`
- Modify: `app/desktop/DesktopLogSink.cpp`
- Modify: `src/platform/RotatingLogWriter.cpp`
- Test: `tests/integration/RotatingLogWriterTests.cpp`
- Test/Create after CMake inspection: teste dedicado de `DesktopLogSink`

#### Step 1: Escrever reproducer de append lento e teardown concorrente

Provar que nao existe uso do sink depois do destrutor e que a desinstalacao nao
espera indefinidamente por I/O.

#### Step 2: Apresentar o desenho de lifetime

Nao liberar o mutex mantendo raw pointer sem ownership. Escolher lifetime token
ou fila limitada somente depois do reproducer.

#### Step 3: Corrigir I/O sob mutex

Manter sob lock apenas instalacao, desinstalacao e captura de estado seguro.

#### Step 4: Corrigir truncamento UTF-8

Encontrar a ultima fronteira valida de code point que caiba com o sufixo. Nao
substituir byte invalido silenciosamente.

#### Step 5: Validar

Run:

```bash
ctest --preset dev --output-on-failure -R "rotating log|log sink|help about"
```

Expected: PASS, sem warning Qt fatal.

### Slice 3: Widths por key e fronteiras pequenas

**Files:**

- Modify: `src/presentation/SsaColumnDisplayCatalog.cpp`
- Modify: `src/query/SqlQueryBuilder.cpp`
- Modify: `src/infra/sqlite/SqliteSsaRepository.cpp`
- Modify only if needed: `src/presentation/BrowseRequestCoordinator.cpp`
- Tests: unitarios/presentation correspondentes

#### Step 1: Parear key e width

Substituir o array posicional por entradas `{key, width}` e validar cobertura
integral contra `ColumnCatalog::all()`.

#### Step 2: Mover query de derivadas para query builder

Preservar bindings, ordenacao e colunas retornadas.

#### Step 3: Remover cast fragil sem wrapper novo

Preferir acesso tipado ao estado booleano necessario; se isso ampliar API sem
ganho, manter a invariante documentada e testar.

### Slice 4: Desenho e primeiro corte dos hubs de importacao

**Files:**

- Review/Modify: `src/infra/import/SpreadsheetImportWorkflowPort.cpp`
- Review/Modify: `src/infra/import/ImportFileStager.cpp`
- Review: `src/infra/sqlite/SqliteSsaImportWriter.cpp`
- Test: `tests/integration/SpreadsheetImportWorkflowPortTests.cpp`

#### Step 1: Mapear responsabilidades e estado compartilhado

Listar inputs, outputs e invariantes de cada bloco de
`importDiscoveredFiles()`. Nao editar ainda.

#### Step 2: Extrair somente processamento de workbook/chunks

O resultado deve conter mapping status, contagens e conflitos. Nao deve mover
fontes nem publicar journal.

#### Step 3: Reexecutar contratos de atomicidade

Crash, cancelamento, falha de writer, journal pendente e retomada devem manter
os mesmos resultados e diagnosticos.

#### Step 4: Avaliar segundo corte

Somente se o primeiro corte reduzir coesao ruim sem duplicar estado, separar
publicacao/consolidacao. Nao criar port generico.

### Slice 5: Medicao de I/O e complexidade

Medir antes de otimizar:

- normalizacao de 250 mil linhas ja canonicas; concluida em 30 amostras por
  cenario no fechamento de 2026-07-18;
- conexao SQLite cold/warm por read;
- filename adversarial;
- profiling valido do prefetch; CPU/idle ja foram isolados no harness.

Registrar mediana, p95, CPU e RSS. A2 entrou como patch mecanico pequeno,
mas nao deve justificar uma refatoracao ampla de `SsaImportPolicy`.

### Slice 6: Test debt por suite

Ordem:

1. `SqliteRepositoryTests`: backoff e polling.
2. `FilterPanelSmokeTest`: assertions negativas de 400ms.
3. paths com I/O real em `/tmp` para `QTemporaryDir`.
4. `SqliteDatabaseValidatorTests`: remover contador static.
5. `HelpAboutQmlTest`: diagnostico de screenshot.

Nao trocar waits que simulam uma operacao lenta necessaria para testar
cancelamento sem criar primeiro um seam deterministico equivalente.

### Slice 7: Retomar pendencias anteriores

Depois dos slices acima:

1. pointer real por familia de menu;
2. geometria unica e URL multiplataforma do grafo;
3. profiling valido do prefetch;
4. clang-tidy, qmltypes e Semgrep parser/timeout;
5. TOCTOU de consolidacao por handle;
6. decisoes de produto sobre parsing e paridade Python.

## 7. Gates desta conversa

- Review imediato apos cada edicao.
- Nao executar clang-tidy em alteracao somente de teste/documentacao.
- Nao executar qmlformat sem mudanca QML.
- detect-secrets por arquivo alterado.
- Gitleaks e TruffleHog uma vez no fechamento do slice.
- CodeRabbit uma vez no fechamento do slice.
- CTest focado durante o slice; CTest completo somente no gate final ou a cada
  tres slices estaveis.
- Timeout nunca conta como sucesso.

## 8. Primeiro comando da proxima conversa

```bash
git status --short --untracked-files=all
git rev-parse HEAD
git describe --tags --always
```

Depois, ler nesta ordem:

1. `AGENTS.md`.
2. `ROUND_STATUS.md`.
3. `RECOVERY_BACKLOG.md`.
4. Este handoff.

Primeira atividade seguinte: `TEST-DETERMINISM-DELTA`, com inventario por suite,
RED/GREEN focado e repeticao antes da matriz completa. O segundo slice deve ser
`IMPORT-FILENAME-TIMESTAMP-SCAN` com 30 processos e relatorio p50/p95.
