# v0.9.10 Follow-up Implementation Plan

> **For Claude:** Use `${SUPERPOWERS_SKILLS_ROOT}/skills/collaboration/executing-plans/SKILL.md` to implement this plan task-by-task.

**Goal:** Incorporar feedback externo e fechar as pendencias reais pos-v0.9.10 sem regredir importacao, camadas C++, GUI ou supervisor.

**Architecture:** Preservar core/domain sem Qt ou SQLite, presentation sem SQL e infra sem decisao visual. Executar slices atomicos no `master`, com TDD focado, review antes do build e publicacao em GitLab e Bitbucket somente quando autorizada.

**Tech Stack:** C++20, Qt 6.11, QML, SQLite, Catch2, QtTest, CMake presets `dev`/`release`.

## Gate de estabilizacao v0.9.11 - 2026-07-18

- Build canonico verde apos reconfiguracao `dev`; warning miniz e preexistente
  em dependencia cacheada.
- CTest sequencial completo passou `602/602` em `64.93 s`, inclusive checks
  de versao GUI/CLI, importacao, SQLite, derivadas, QML, memoria e smokes.
- Semgrep amplo (`24` regras/`512` arquivos), Gitleaks (`1.43 GB`) e
  TruffleHog passaram sem finding. A tag anotada `v0.9.11^{}` confirma
  `84462d4`; Bitbucket `master` confirma `6a0bfa4`, que registra a publicacao
  do gate.
- GitLab `origin` recusou push por OAuth `invalid_grant`, dependencia externa
  sem alegacao de publicacao.

## Recheck UX-GRID-ALIGN - 2026-07-18

- A reproducao offscreen atual em `1580x940` nao reproduziu desalinhamento de
  Macro ou Reprogramacoes. Os cards exibem baseline, padding e altura visual
  coerentes com os cards de texto; nenhum QML foi alterado.
- O CTest `ssa_qml_advanced_popup_tests` passou `1/1` em `1.58 s`. Resta
  reproducer no monitor afetado ou contrato geometrico no mesmo `Flow` antes
  de fechar o item ou alterar layout.

## Analytics atomico na importacao incremental - 2026-07-18

- `44411da` acrescenta prova fail-closed do workflow: schema invalido de
  `activity_analytics_point` produz diagnostico analytics, status `Failed` e
  rollback antes de qualquer SSA, journal, snapshot ou meta parcial publicar.
- O target manual `run_activity_analytics_benchmark_250k` usa agora o caminho
  real `rescan -> finishWithAnalytics`. Base de 250000 linhas, no-op com o
  mesmo XLSX restaurado e delta de uma linha deram `12239.400 ms`,
  `10338.718 ms` e `2514.052 ms`; o contrato terminou em
  `250000 -> 250000 -> 250001`, seis snapshots, nove pontos e SPG `250001`.
  O RSS adicional foi `121978880` bytes, abaixo de 256 MiB.
- Validacao local: CTest focado `28/28` em `14.67 s`; formatacao, cppcheck,
  Semgrep e detect-secrets limpos. clang-tidy saiu zero sem aviso novo no diff,
  embora mantenha avisos baseline de Catch2 e teste existente. Bitbucket
  confirmou `44411da`; GitLab `origin` continua OAuth `invalid_grant`.
- Nenhuma otimizacao de producao foi aplicada. Pular fingerprint/capture por
  filename ou `rowsWritten` pode ignorar mutacao externa, reparo de schema ou
  mudanca de data. O proximo desenho somente sera considerado se um revision
  ledger transacional cobrir cada escritor de `ssa_table`.
- Contadores permanecem: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder, fila `5/8 = 62.5%`.

Proxima atividade unica: mapear escritores e fronteiras de transacao de
`ssa_table` para decidir a viabilidade de revision ledger antes de schema ou
cache de producao.

## UX-NAV: cadeia fixa e erros concorrentes - 2026-07-18

- `08991b6` faz a tabela ser dona da cadeia de derivadas. Navegacao interna
  atualiza somente detalhes; nova selecao, lookup externo e clear descartam
  respostas stale por ID e geracao.
- Record e filhas diretas nao compartilham mais erro mutavel. A prioridade de
  Record e exibida antes de loading em QML, e o indice de relacao preserva
  ocorrencias duplicadas da mesma SSA.
- REDs cobriram cadeia fixa, filhas pendentes, duplicata e dois erros
  concorrentes. Review Terra ultra corrigiu P1/P2 e terminou `SEM FINDINGS`.
- Gate: static checks limpos; targets afetados; CTest `2/2` em `14.87 s`;
  `all_qmllint` e hook staged passaram. Sem schema/layout; contadores seguem
  plano `99.0/100`, divida `13/14 = 92.9%`, legado `0.0/100`, fila `5/8 = 62.5%`.
- Sync completa de derivadas permanece pendente de cinco decisoes de produto:
  prioridade de fonte, multiparent, ciclos, orfaos e desativacao. Sem elas,
  nao e seguro migrar o modelo escalar atual para procedencia/closure externa.

Proxima atividade unica: gate completo do candidato `0.9.11`; criar tag apenas
se a suite e as verificacoes de release passarem.

## Checkpoints causais privados de parsing e merge de derivadas - 2026-07-18

- `77fd3e0` troca tres waits temporais por checkpoints reais: 4 KiB ja
  processados em linha delimitada e primeira edge ja aceita pelo merger dentro
  de `SqliteDerivadasPort`. Os contratos pedem stop somente depois do sinal e
  exigem banco vazio/reutilizavel.
- P2 de sinais em API publica e P1 de merge somente isolado apareceram no
  primeiro review. O GREEN final usa `DerivadasImportTestAccess` em `tests/`,
  overloads privados por chamada e nenhum semaforo/callback no contrato publico.
  Dois re-reviews Terra ultra finais: `SEM FINDINGS`.
- Validacao: gate estatico limpo, build `ssa_integration_tests`, causal `3/3`
  em `0.08 s`, repeticao `90/90` em `1.90 s`, derivadas `22/22` em `0.49 s`.
  Bitbucket confirma `77fd3e0`; GitLab `origin` continua OAuth `invalid_grant`;
  clawpatch local permanece indisponivel por tooling/modelo incompativel.
- Sem schema, SQL, regra ou API publica nova. Limite residual: cancelamento e
  cooperativo e nao interrompe I/O de SO ja bloqueado em `std::getline`.
  Contadores sem inflacao: plano original `99.0/100`, divida nova `13/14 =
  92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: diagnosticar a sincronizacao completa de derivadas
pendente com regras de negocio e fonte externa antes de editar schema ou UI.

## Cancelamento causal de derivadas sob SQLite bloqueado - 2026-07-18

- `6e818c0` substitui dois waits de `50ms` por contencao SQLite observavel.
  `SqliteDerivadasPort` retem sinal opcional e passa ao handler local; o
  primeiro `SQLITE_BUSY` libera o teste antes do stop e do ROLLBACK.
- O patch cobre importacao e cleanup de orfas, sem mudar schema, SQL, API do
  port ou comportamento normal. O RED encontrou a API ausente; ajustes de
  libc++ macOS para default de struct e semaforo inicial `0` foram corrigidos.
- Validacao: gates estaticos limpos, build afetado, `2/2` em `0.07 s`,
  `60/60` em `2.57 s`, derivadas `22/22` em `0.61 s`, SQLite Repository
  `31/31` em `0.76 s`; review Terra ultra `SEM FINDINGS`. Bitbucket confirma
  `6e818c0`; GitLab `origin` continua OAuth `invalid_grant`; clawpatch falhou
  por CLI/modelo incompativel.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: transformar em checkpoints locais os waits de parsing
e merge de derivadas, sem misturar com a fase SQLite.

## Recovery de staged journalizado antes de rescan - 2026-07-18

- `1d04ab0` muda somente a ordem de recovery no `rescan()`: journal pendente
  e retomado sob lock antes da limpeza de `.ssa-staged-*`. Isto preserva a
  copia owned que uma importacao externa precisa para concluir move pos-commit
  depois de crash.
- RED de processo: source `.ssa-staged-crashed_123_0.xlsx` falha no baseline
  porque o cleanup a remove e deixa journal irresumivel. Green prova destino em
  `processadas/` e journal vazio; artifacts sem journal continuam removidos.
  A private `importIncrementalFiles` sem call site foi removida.
- Validacao: build afetado, `5/5` em `0.47 s`, workflow `174/174` em `9.43 s`
  e crash `20x` em `1.07 s`; gates estaticos limpos, review Terra ultra
  `SEM FINDINGS`. clang-tidy so trouxe avisos preexistentes; clawpatch local
  falhou por CLI/modelo incompativel. Bitbucket confirma `1d04ab0`; GitLab
  `origin` continua OAuth `invalid_grant`.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: eliminar espera temporal do cancelamento SQLite de
derivadas com checkpoint de contencao real.

## Handshake causal dos crash probes SQLite - 2026-07-18

- `6bcf65e` substitui os tres polls de marker/journal por handshake `READY\n`
  no stdout. O token e escrito somente apos o commit do marker `QSaveFile`; o
  helper de teste acumula bytes para evitar dependencia de framing do processo.
- No unico checkpoint bloqueado, `pendingConsolidation()` e separado do writer
  observado. O semaforo e conectado apenas a `completeConsolidation`, provando
  busy do DELETE/COMMIT antes do kill. A `jthread` usa `stop_callback` para
  acordar em erro e nao bloquear seu destrutor; `-journal` continua asserido.
- RED `3/3` falhou sem token. P1 de sinalizacao prematura na leitura previa foi
  corrigido; review Terra ultra final `SEM FINDINGS`. Validacao: build afetado,
  CTest causal `3/3` em `0.36 s`, workflow `174/174` em `7.38 s` e journal
  repetido `10x` em `0.78 s`; gates estaticos limpos. Bitbucket confirma
  `6bcf65e`; GitLab `origin` continua OAuth `invalid_grant`.
- `clawpatch` local ficou indisponivel por incompatibilidade de configuracao
  externa, portanto nao e citado como validacao. Contadores sem inflacao:
  plano original `99.0/100`, divida nova `13/14 = 92.9%`, legado `0.0/100`
  placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: selecionar por risco a proxima pendencia local de
confiabilidade de importacao/SQLite fora do denominador fixo.

## Cancelamento causal do conversor legacy - 2026-07-18

- `0172f2e` remove a espera manual de sidecar no contrato de cancelamento legacy.
  O fake runner injetado cria output parcial, libera um `binary_semaphore` e
  observa `stop_token` por `stop_callback`, sem alterar o conversor real.
- O teste preserva destino anterior e prova cleanup do diretorio temporario. A
  cobertura de spawn real continua na suite do supervisor; include e fake branch
  sem uso foram removidos. Review Terra ultra final: `SEM FINDINGS`.
- Validacao: build de `ssa_integration_tests`, familia legacy `4/4` em `0.08 s`,
  workflow `174/174` em `6.85 s`, gates estaticos limpos e hooks staged verdes.
  Bitbucket confirma `0172f2e`; GitLab `origin` segue OAuth `invalid_grant`.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: remover polling dos crash probes SQLite usando
checkpoint causal sem alterar kill, journal ou recovery.

## Checkpoint causal de copia/staging - 2026-07-18

- `79f8200` substitui sete loops que aguardavam `.part` por uma seam causal de
  primeira escrita. O callback opcional percorre copy, stager e workflow; sem
  callback, o comportamento de producao e identico.
- Os contratos agora provocam de forma sincrona cancelamento, fonte alterada,
  fonte removida, inventario parcial, importacao externa e falha de cleanup. O
  RED de callback que lanca reproduziu uma excecao escapando; P2 de cleanup foi
  corrigido para resultado `Failed`/`CleanupFailed` com tentativa explicita de
  remover o temporario. Review final Terra ultra: `SEM FINDINGS`.
- Validacao: build de `ssa_integration_tests`, CTest focado `8/8` em `0.80 s`,
  workflow `174/174` em `8.65 s`, gates estaticos limpos e hooks staged verdes.
  Bitbucket confirma `79f8200`; GitLab `origin` segue OAuth `invalid_grant`.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: remover polling de processo do conversor legacy com
handshake causal, sem refactor de schema, handles ou regras de negocio.

## Polling de staging e corpus lock removido - 2026-07-18

- `1f99a90` fecha tres waits temporais em contratos de importacao, sem tocar em
  producao. `writerBusyEntered` prova staging completo e contencao SQLite antes
  de cancelar, alterar permissao de cleanup ou verificar o corpus lock.
- Foram removidos polling de diretorio e `QElapsedTimer`/`msleep(5)`, alem do
  wait fixo de `50 ms`. Continuam cobertos workbook staged, cleanup owned,
  rollback e exclusao de `QLockFile` concorrente.
- Review Terra ultra encontrou P1 na primeira versao: `snapshotLocked` so
  sinaliza publication, enquanto #456 fica no preflight. A correcao usa
  `writerBusyEntered`; review final: `SEM FINDINGS`.
- Validacao: build de `ssa_integration_tests`, CTest focado `3/3` em `0.48 s`,
  workflow `174/174` em `7.72 s`, gate estatico limpo. Bitbucket confirma
  `1f99a90`; GitLab `origin` segue OAuth `invalid_grant`.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: substituir o proximo polling de staging quando houver
evento causal de copia ou conversao, sem refactor de producao especulativo.

## Sinais causais de contencao SQLite na importacao - 2026-07-18

- `f62ea53` fecha o slice de waits temporais de `sqliteBusyWait` em importacao
  e rescan. O writer expoe um sinal opcional com ownership seguro na sessao; o
  workflow separa `writerBusyEntered` de `snapshotLocked` da publication.
- Review Terra ultra encontrou dois P2 reais: permit residual de cleanup e
  dupla liberacao entre `SQLITE_BUSY` e `SQLITE_LOCKED`. O patch consome o
  primeiro evento e compartilha um unico `atomic_flag`; review final retornou
  `SEM FINDINGS`.
- Validacao: gate estatico limpo; build afetado; CTest causal `7/7` em `0.56 s`,
  workflow `174/174` em `7.96 s` e SQLite Repository `31/31` em `0.72 s`.
  Bitbucket confirma `f62ea53`; GitLab `origin` segue OAuth `invalid_grant`.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: substituir polling de staging e corpus lock por eventos
causais nos contratos de importacao restantes.

## Benchmark da formatacao de pagina - 2026-07-18

- `ce90132` introduz `ssa_table_page_format_benchmark` e smoke CTest para a
  pagina maxima de `500x12`, com colunas reais de texto, inteiro e data. A
  fixture e preconstruida para separar formatacao de query, I/O e paint QML.
- Resultado de 30 amostras, 10 iteracoes cada: wall p50/p95
  `2.93215/3.3340834 ms` e CPU p50/p95 `2.9244/3.2649 ms` por pagina. JSON
  local nao versionado: `build/dev/table-page-format-benchmark-30.json`.
- O formatter ja esta no worker; nao existe gargalo que justifique mudar cache,
  contrato `SsaTableDisplayValues` ou layout por lazy formatting. P7 fica
  resolvido localmente por medicao, sem refactor especulativo.
- Gate estatico limpo, target afetado verde, smoke `1/1` em `0.04 s` e review
  Terra ultra `SEM FINDINGS`. Bitbucket confirma `ce90132`; GitLab `origin`
  permanece OAuth `invalid_grant`; local 74 commits a frente.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: trocar as esperas temporais de contencao SQLite nos
contratos de importacao/rescan por sinais causais de `sqliteBusyWait`.

## Reconciliacao do macro report - 2026-07-18

- P4 esta obsoleto: o macro report do HEAD ja usa analytics SQL com
  `COUNT(DISTINCT "ssa_number")`, sem o antigo mapa/set em memoria.
- CTests de SQL, filtros completos e agrupamento passaram `3/3` em `0.13 s`.
  O gargalo foi refutado; escala futura segue sem benchmark especifico.
- Contadores sem mudanca: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila `5/8 = 62.5%`.

Proxima atividade unica: medir formatacao eager da tabela antes de alterar GUI
ou cache.

## Determinismo causal do repositorio SQLite - 2026-07-18

- `28700b3` elimina polling de lock e waits de `10/50 ms` nos contratos SQLite;
  usa sinais de lock adquirido, busy callback e progress callback. A seam e
  opcional, owned por `shared_ptr<counting_semaphore>` e nula em producao.
- P2 de lifetime/overflow e P1 de tipo foram encontrados durante o ciclo e
  corrigidos; review final Terra ultra: `SEM FINDINGS`.
- Validacao: gate estatico limpo, familia SQLite Repository `31/31` em `0.68 s`
  e hooks staged de formatacao, Gitleaks, detect-secrets e TruffleHog verdes.
- Bitbucket confirma `28700b3`; GitLab `origin` continua OAuth `invalid_grant`;
  local `71` commits a frente. Contadores sem inflacao: plano original
  `99.0/100`, divida nova `13/14 = 92.9%`, legado `0.0/100` placeholder e fila
  operacional `5/8 = 62.5%`.

Proxima atividade unica: medir equivalencia e custo do macro report
`COUNT(DISTINCT)` antes da migracao para SQL.

## Indice status-last de producao - 2026-07-18

- `4396e1c` instala pelo writer o indice de expressao DESC equivalente ao
  `ORDER BY` status-last. O indice e criado apenas quando existem `numero_ssa`
  e `situacao`; DBs antigos o recebem na proxima escrita, sem schema migration
  de leitura.
- Testes provam DDL/plano, recriacao, tabela customizada legacy e preservacao
  fail-closed de indice homonimo alheio. O primeiro review Terra ultra apontou
  dois P2 corrigidos; review final: `SEM FINDINGS`.
- Gates estaticos limpos; Semgrep `62` regras/zero finding. Build afetado passou
  e CTest direto `38/38` em `1.14 s`. Bitbucket confirma `4396e1c`; GitLab
  `origin` segue OAuth `invalid_grant`; local `69` commits a frente.
- Contadores sem inflacao: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado `0.0/100` placeholder e fila operacional `5/8 = 62.5%`.

Proxima atividade unica: substituir polling temporal de `SqliteRepositoryTests`
por sinais causais de lock, busy e progress SQLite.

## Benchmark SQLite status-last - 2026-07-18

- `1d950e3` entrega o harness isolado com fixture de `250000` linhas e `30`
  amostras; JSON local nao versionado:
  `build/dev/sqlite-status-last-benchmark-30.json`.
- O baseline usa `TEMP B-TREE`; a variante indexada usa
  `idx_ssa_table_status_last_numero_ssa_desc`, sem `TEMP B-TREE`, com vetores
  de resultados identicos.
- Wall p50/p95 baseline/indexado: `41.260958/45.094667 ms` versus
  `0.0235/0.030125 ms`. CPU p50/p95: `41.184/44.742 ms` versus
  `0.024/0.031 ms`.
- Gates estaticos limpos; smokes `2/2` em `0.37 s`; Terra ultra final:
  `SEM FINDINGS`. O escopo e fixture SQL isolada: nenhum indice de producao,
  nenhuma prova de filtros arbitrarios ou GUI completa.
- Publicacao historica: Bitbucket `0/0` em `1d950e3`; GitLab `origin` segue
  em OAuth `invalid_grant`; local `67` commits a frente de `origin/master`.
- Contadores sem mudanca: plano original `99.0/100`, divida nova
  `13/14 = 92.9%`, legado placeholder `0.0/100`, fila operacional `5/8 = 62.5%`.

Proxima atividade unica: RED de integracao e indice DESC de expressao no writer.

## Cobertura sintetica de URL Windows/UNC - 2026-07-18

- HEAD anterior: `e76e722`. O commit `bca795a` entrega somente em
  `AdvancedPopupQmlTest.cpp` um contrato para `file:///C:/SSA/graph.png` e
  `file://server/share/graph.png`: `DerivadasGraphModel::localFilePath()`
  delega exatamente para `QUrl::toLocalFile()`. Na publicacao de `bca795a`,
  Bitbucket foi confirmado com divergencia `0/0`; GitLab `origin` falhou por
  OAuth `invalid_grant`, e o HEAD local estava 65 commits a frente de
  `origin/master`.
- Validacao local: `ssa_qml_advanced_popup_tests` `1/1` em `2.16 s`;
  `git diff --check`, clang-format e detect-secrets passaram; Semgrep executou
  53 regras com zero finding. Review Terra ultra terminou sem findings.
- Sem producao e sem credito: plano original `99.0/100`; divida nova
  `13/14 = 92.9%`; backlog legado `0.0/100` como placeholder; fila operacional
  `5/8 = 62.5%`.
- Risco residual: cobertura sintetica nao prova SMB, `CreateFileW`, reparse
  points, handles de diretorio ou gravacao em share. Proxima atividade unica:
  prova Windows/UNC real para importacao e grafo. Profiling do prefetch continua
  bloqueado.

## Fechamento local de tooling - 2026-07-18

- Branch `master`; HEAD de codigo `2e2476b`, Bitbucket `0/0`; GitLab bloqueado
  por OAuth `invalid_grant`; local 63 commits a frente de `origin/master`.
- Contadores atuais: plano original `99.0/100`; divida nova `13/14 = 92.9%`;
  backlog legado `0.0/100` como placeholder; fila operacional `5/8 = 62.5%`.
- `CMAKE_OSX_SYSROOT=macosx` esta nos cinco presets-raiz, e `dev-arrow` herda
  `dev`. O compile database recebeu SDK macOS 26.5; `clang-tidy` direto passou
  em `2.50 s`. `ssa_infra` passou 58 passos em `5.55 s`, integracao 160 em
  `13.19 s` e mapper `15/15` em `0.44 s`.
- Semgrep 1.170 focado no arquivo de importacao com 6,815 linhas passou em
  `2.92 s`, com duas regras atuais elegiveis, zero finding e zero timeout.
  `SEMGREP-WIDE-NARROW-TIMEOUT` foi refutado localmente: `63554b9` nao registra
  IDs, YAML, fixture ou log. Nao dividir fixtures. A equivalencia exata ao
  Semgrep 1.169 do CI depende de socket Docker local, hoje ausente.
- Proxima atividade unica: prova Windows/UNC em plataforma real para importacao
  e grafo. Profiling valido do prefetch continua bloqueado; handles, PowerShell
  e packaging seguem dependentes de plataformas reais.

## Fechamento de determinismo e filename - 2026-07-18

- Branch `master`; HEAD de codigo `be20b99`. Bitbucket esta em `be20b99` com
  divergencia `0/0`; GitLab continua bloqueado por OAuth `invalid_grant`. Antes
  do commit documental, o local estava 61 commits a frente de `origin/master`.
- Contadores atuais: plano original `99.0/100`; divida nova
  `13/14 = 92.9%`; backlog legado `0.0/100` como placeholder; fila operacional
  `4/8 = 50.0%`.
- Commits de `785c73e` ate `bcb7980` fecham `TEST-DETERMINISM-DELTA`. A matriz
  focada passou `26/26` em `28.38 s`; o mapper passou `30/30` em `2.21 s` e
  preserva uma janela temporal deliberada. Waits residuais aguardam eventos
  reais de filesystem, processo, SQLite, timer ou cancelamento. Timeout nao foi
  aceito como sucesso.
- `be20b99` fecha `IMPORT-FILENAME-TIMESTAMP-SCAN`: target/build, gate `3/3`
  em `0.41 s`, recheck `3/3` em `0.13 s`, 30 processos, 12,000 parses e
  `2.00 s`. P95 ISO `783.3 ns`; maximo adversarial `178269.15 ns`, ou
  `0.178 ms`. O parser e O(n); nenhuma otimizacao foi aplicada.
- CTest sequencial `595/595` em `66.86 s`. O registro atual possui 596 testes
  porque o smoke de filename foi adicionado depois desse full.
- Security ampla: Semgrep `509` arquivos/`24` regras/zero finding, Gitleaks
  `1.42 GB`/zero leak e TruffleHog `9,226` chunks/zero segredo.
- Restam: profiling valido do prefetch por 1.0 ponto original, bloqueado pela
  ferramenta; tooling clang-tidy/Semgrep; `IMPORT-STAGER-HUB` em Windows/UNC;
  handles, PowerShell e packaging reais.
- Proxima atividade unica: canonizar clang-tidy macOS com sysroot e reproduzir
  IDs Semgrep. Depois, executar as provas externas enquanto profiling segue
  bloqueado.

## Historico anterior: atualizacao operacional - 2026-07-17

Fechamento posterior em 2026-07-18:

- HEAD de codigo `b2369ac`, precedido por `7291082`, `0025d44` e `7f396b0`,
  estabiliza os gates temporais, fecha o snapshot WAL e propaga
  `sqliteBusyWait` por recovery e journal cleanup.
- Em `4e74790`, CTest sequencial passou `581/581` em `82.06 s`; security ampla
  ficou limpa.
- Contadores: plano original `99.0/100`, divida nova `78.6/100` por 11/14 e
  backlog legado `0.0/100` sem denominador aceito.
- Controle operacional complementar: `2/8 = 25.0%`. Os dois pacotes SQLite
  atuais estao fechados. Determinismo e filename benchmark elevam a fila para
  `4/8 = 50.0%` e a divida nova para `13/14 = 92.9%`.
- Churn SQLite real: first read wall p50/p95 `6.948/12.897 ms`;
  open/read/close repetido `1.390/3.296 ms`; 2,400 chamadas concorrentes de
  `page()` ficaram em `157.501/219.458 ms`, sem falha ou cancelamento. Pool nao
  foi justificado e o cache de paginas do SO nao foi controlado.
- No marco intermediario `4e74790`, Bitbucket foi confirmado e GitLab ficou
  bloqueado por OAuth `invalid_grant`.
- A publicacao do rescan agora usa SQLite Backup API no destino. Os 3 REDs
  passaram `3/3` em `0.22 s`, o gate ampliado passou `7/7` em `0.35 s` e o
  review Sol fechou a corrida de stop tardio apos `SQLITE_DONE`.
- `EXPLAIN QUERY PLAN` confirmou que `COLLATE NOCASE` impede seek no indice
  BINARY de `numero_ssa`; count/rows cairam de `43/71 ms` para `2/<1 ms` no
  experimento read-only.
- `1cea684` restringe BINARY a SSA canonica; `3/3` e `45/45` passaram. Em 30
  processos, 2,400/2,400 reads foram corretos e `page()` p50/p95 ficou em
  `0.133/0.229 ms`, contra `157.501/219.458 ms`.
- `360d18b` retira QLockFile, I/O SQLite e log do mutex do resumo de derivadas.
  O RED falhou em `1.06 s`; o GREEN passou 2/2, 60 repeticoes, `13/13` e
  `8/8`. Sol xhigh e Terra high deram GO apos fechar 1 P2 de cobertura.
- Os oito testes do registro antigo de 564 casos passaram `8/8` em `10.09 s`;
  a matriz atual possui 588 casos.
- Na rodada atual, os oito nomes passaram `8/8` em `9.77 s`; os tres contratos
  SQLite passaram 30 execucoes e a familia ampliada passou `15/15` em `2.67 s`.
- `610fbf3` prova o snapshot WAL antigo durante publicacao e o novo snapshot na
  transacao seguinte.
- `7f396b0`: conversor 20/20, SAM 20/20 suites, Details 10/10, CurrentWeek
  20/20 e CTest final `588/588` em `78.30 s`. Semgrep, Gitleaks e TruffleHog
  amplos ficaram limpos.
- `7291082` foi confirmado no Bitbucket com divergencia `0/0`; o push GitLab
  continua bloqueado por OAuth `invalid_grant`.
- `b2369ac` fecha `SQLITE-SCHEMA-VERSION-GATE`: legado zero e versao atual sao
  aceitos, futuras falham antes de DDL/DML ou movimento de recovery, o stamp
  ocorre na write transaction e o validator usa snapshot de leitura unico.
- RED de recovery confirmado; contratos principais `80/80`, cleanup direto
  `30/30`, gate amplo `45/45` e CTest final `595/595` em `75.81 s`. A primeira
  matriz `590/594` foi investigada sem aceitar timeout: os quatro casos passaram
  isolados e `40/40` repeticoes antes do full limpo.
- Security atual: Semgrep 508 arquivos/24 regras, Gitleaks 1.42 GB e TruffleHog
  Git 9,090 chunks, sem finding. Bitbucket confirmou `b2369ac` com `0/0`;
  GitLab continua bloqueado por OAuth `invalid_grant`.
- Proxima atividade: fechar `TEST-DETERMINISM-DELTA` e depois executar o
  benchmark formal de filename. Profiling permanece o terceiro slice porque a
  ferramenta local ainda nao gerou evidencia valida.

Historico de marcos intermediarios anteriores ao fechamento acima:

- Commit `e0b5401` publicado em `origin` e `bitbucket`.
- Commit `41ecf36` tambem publicado em `origin` e `bitbucket`.
- Commit `c95a757` tambem publicado em `origin` e `bitbucket`.
- Commits estruturais `14485a9`, `627a075`, `2c7a35f` e `55c6bd8` foram
  publicados no Bitbucket. GitLab esta pendente por OAuth `invalid_grant`.
- Dois polls sincronos de `quickSector()` foram removidos; o resumo baseado em
  timer foi preservado.
- Dois polls sincronicos de `textFilter()` foram removidos; os resumos baseados
  em timer permaneceram.
- Um poll sincronico de `sector->quickSector()` foi removido; timers de resumo
  permaneceram.
- Build e CTest da suite `ssa_qt_filter_panel_tests`: `1/1`, `2.96 s`.
- Validacao adicional da suite: `1/1`, `5.74 s`.
- Validacao adicional final: `1/1`, `2.88 s`.
- O workflow hub foi fechado com resultado tipado para SAM e XLSX/chunks.
- O stager hub foi separado localmente: stager com discovery/copias owned e
  consolidator com moves post-commit; Windows/UNC real segue pendente.
- Gates: `11/11` focados e `453/453` completos em `65.75 s`.
- Contadores auditados: plano original `99.0/100`, divida nova `64.3/100`
  (9/14 aceitos) e backlog legado sem denominador definido.

---

## Baseline e invariantes

- Baseline funcional antes do commit documental: `d34f92d` em `master`.
- Suite local autoritativa: 443/443 testes verdes.
- Schema SQLite e chaves internas permanecem ASCII canonico.
- Nenhum movimento de fonte pode ocorrer antes de banco e journal duraveis.
- N3 permanece fail-closed sem prova positiva do SO.
- Nenhum layout, label ou action ID muda sem requisito explicito.
- Untracked locais, configuracoes pessoais e artefatos nao entram no Git.

### Task 1: Triar feedback de Cursor GLM e OpenCode

**Files:**

- Modify: `ROUND_STATUS.md`
- Modify: `RECOVERY_BACKLOG.md`
- Reference: `docs/plans/2026-07-17-v0.9.10-follow-up.md`

#### Step 1: Registrar cada finding sem editar codigo

Criar uma tabela temporaria no report da rodada com origem, arquivo/linha,
severidade alegada, contrato afetado e evidencia fornecida.

#### Step 2: Reproduzir ou inspecionar no HEAD

Usar `rg`, testes existentes e o menor reproducer focado. Nao aceitar finding
por autoridade da ferramenta.

#### Step 3: Classificar

Marcar cada item como valido, duplicado, obsoleto, falso positivo, dependencia
externa ou fora de escopo. Findings validos entram no backlog com prioridade,
aceite e arquivos previstos.

#### Step 4: Atualizar os dois documentos de controle

`ROUND_STATUS.md` recebe fatos da rodada; `RECOVERY_BACKLOG.md` recebe somente
trabalho residual. Nao duplicar itens resolvidos.

#### Step 5: Review documental

Run: `markdownlint ROUND_STATUS.md RECOVERY_BACKLOG.md docs/plans/2026-07-17-v0.9.10-follow-up.md`

Expected: PASS.

### Task 2: Completar pointer real por familia de menu

**Files:**

- Modify: `tests/smoke/AdvancedPopupQmlTest.cpp`
- Modify: `tests/smoke/PresentationSmokeTest.cpp`
- Modify only if a defect is proven: `app/desktop/qml/Main.qml`
- Modify only if a defect is proven: `app/desktop/qml/components/SsaTable.qml`

#### Step 1: Mapear familias ainda sem pointer

Cobrir importacao/exportacao, manutencao/banco, header e grafo. Reusar testes de
handler existentes sem duplicar geometria para cada action.

#### Step 2: Escrever um teste RED por familia

Cada teste deve abrir o menu com mouse real, clicar a action e observar request
ou estado terminal, sem `invokeMethod("click")` no contrato de pointer.

#### Step 3: Executar teste focado

Run: `ctest --preset dev --output-on-failure -R "ssa_qml_advanced_popup_tests|ssa_qt_presentation_tests"`

Expected: FAIL somente no contrato novo antes da correcao.

#### Step 4: Implementar o minimo se houver defeito de producao

Preservar labels, IDs, layout e `Theme.clampedPopupX/Y`.

#### Step 5: Medir offscreen e remover instrumentacao

Run: `QT_QPA_PLATFORM=offscreen QT_FATAL_WARNINGS=1 ctest --preset dev --output-on-failure -R "ssa_qml_advanced_popup_tests|ssa_qt_presentation_tests"`

Expected: PASS sem `console.info` residual.

### Task 3: Fechar contrato multiplataforma do grafo

**Files:**

- Modify: `src/presentation/DerivadasGraphModel.cpp`
- Modify: `src/presentation/DerivadasGraphModel.h`
- Modify: `app/desktop/qml/components/DerivadasGraph.qml`
- Test: `tests/smoke/AdvancedPopupQmlTest.cpp`

#### Step 1: Escrever casos RED para fonte unica de geometria

Provar que hit-test, arestas, bounds e exportacao usam as mesmas dimensoes de
node sem duplicar `118x48` em C++ e QML.

#### Step 2: Escrever casos de URL

Cobrir `file:///`, espacos, Unicode, Windows drive e UNC sem fallback manual
silencioso.

#### Step 3: Implementar a menor fronteira Qt adequada

Conversao de `QUrl` fica na fronteira Qt; layout continua em presentation e
nao migra para domain.

#### Step 4: Validar modelo real e exportacao

Run: `QT_QPA_PLATFORM=offscreen QT_FATAL_WARNINGS=1 ctest --preset dev --output-on-failure -R "ssa_qml_advanced_popup_tests"`

Expected: PASS para determinismo, bounds, overlap, teclado, SVG e PNG.

### Task 4: Isolar benchmark de prefetch

**Files:**

- Modify: `tests/smoke/PresentationSmokeTest.cpp`
- Create only if measurement cannot stay in the suite: `tools/prefetch_benchmark.cpp`
- Modify only if a target is required: `CMakeLists.txt`

#### Step 1: Medir baseline sem otimizar

Separar startup, latencia ate `succeeded`, CPU ativa, idle, requests e RSS em
30 amostras. Registrar mediana e p95.

#### Step 2: Provar contratos funcionais

Cold page 1, cache hit 2/3, generation/fingerprint, cancelamento e latest-wins
devem permanecer verdes.

#### Step 3: Habilitar profiling somente no target dedicado

Corrigir QML debugging sem habilita-lo no release. Um trace danificado nao e
resultado valido.

#### Step 4: Decidir com numeros

Criar slice de otimizacao somente se a regressao exceder o ruido das amostras.

### Task 5: Reparar gates incompletos

**Files:**

- Modify only after diagnosis: `CMakeLists.txt`
- Modify only after diagnosis: `CMakePresets.json`
- Modify only after diagnosis: `.clang-tidy`

#### Step 1: Reproduzir clang-tidy sem editar

Run: `clang-tidy -p build/dev src/domain/ColumnCatalog.cpp`

Expected: o baseline atual falha ao localizar headers da toolchain; capturar o
comando de compilacao exato.

#### Step 2: Corrigir compile database/toolchain

Nao adicionar includes ao codigo para mascarar configuracao incorreta.

#### Step 3: Gerar `.qmltypes` real

Run: `cmake --build --preset dev --target all_qmllint`

Expected: PASS sem aviso de `ssa_consulta_rapida.qmltypes` ausente.

#### Step 4: Revalidar Semgrep grande

Separar fixtures do arquivo de integracao somente se isso eliminar timeout sem
reduzir cobertura das duas regras narrow/wide.

### Task 6: Desenhar hardening TOCTOU da consolidacao

**Files:**

- Review: `src/infra/import/ImportFileStager.cpp`
- Review: `src/infra/import/SpreadsheetImportWorkflowPort.cpp`
- Review: `tests/integration/SpreadsheetImportWorkflowPortTests.cpp`
- Modify after design approval only: platform-specific consolidation files

#### Step 1: Criar reproducer adversarial controlado

Trocar o diretorio entre validacao e rename sem tocar banco real.

#### Step 2: Comparar primitives por plataforma

Avaliar `openat`/`renameat` em POSIX e handles equivalentes em Windows. Nao
introduzir wrapper generico antes de provar o contrato comum.

#### Step 3: Apresentar desenho antes da implementacao

Parar se a solucao exigir ownership cross-thread, reaper ou API publica nova.

### Task 7: Gate e publicacao do proximo slice

**Files:**

- Modify: `ROUND_STATUS.md`
- Modify: `RECOVERY_BACKLOG.md`

#### Step 1: Review imediato

Executar clang-format/cppcheck/Semgrep/detect-secrets por diff. Qmlformat
somente quando QML mudar; CodeRabbit, gitleaks e TruffleHog uma vez no
fechamento.

#### Step 2: Build e testes

Run: `cmake --build --preset dev`

Run: `ctest --preset dev --output-on-failure`

Expected: 100% PASS; timeout nunca conta como sucesso.

#### Step 3: Atualizar controle

Registrar HEAD, suite real, working tree, remotes, validacao local e
dependencias externas sem misturar os estados.

#### Step 4: Commit e push somente com autorizacao

Publicar `master` em `origin` e `bitbucket`; nao publicar em `gh` enquanto a
conta estiver suspensa.
