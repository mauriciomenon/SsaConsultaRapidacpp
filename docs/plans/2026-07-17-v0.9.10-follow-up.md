# v0.9.10 Follow-up Implementation Plan

> **For Claude:** Use `${SUPERPOWERS_SKILLS_ROOT}/skills/collaboration/executing-plans/SKILL.md` to implement this plan task-by-task.

**Goal:** Incorporar feedback externo e fechar as pendencias reais pos-v0.9.10 sem regredir importacao, camadas C++, GUI ou supervisor.

**Architecture:** Preservar core/domain sem Qt ou SQLite, presentation sem SQL e infra sem decisao visual. Executar slices atomicos no `master`, com TDD focado, review antes do build e publicacao em GitLab e Bitbucket somente quando autorizada.

**Tech Stack:** C++20, Qt 6.11, QML, SQLite, Catch2, QtTest, CMake presets `dev`/`release`.

## Fechamento de determinismo e filename - 2026-07-18

- Branch `master`; HEAD de codigo `be20b99`. Bitbucket esta em `be20b99` com
  divergencia `0/0`; GitLab continua bloqueado por OAuth `invalid_grant`. Antes
  do commit documental, o local estava 61 commits a frente de `origin/master`.
- Contadores atuais: plano original `99.0/100`; divida nova
  `13/14 = 92.9%`; backlog legado `0.0/100` como placeholder; fila operacional
  `4/8 = 50.0%`.
- Commits de `785c73e` ate `bcb7980` fecham `TEST-DETERMINISM-DELTA`. A matriz
  focada passou `26/26` em `28.38 s`; o mapper passou `30/30` em `2.21 s` e
  preserva uma janela temporal deliberada. Waits residuais aguardam eventos
  reais de filesystem, processo, SQLite, timer ou cancelamento. Timeout nao foi
  aceito como sucesso.
- `be20b99` fecha `IMPORT-FILENAME-TIMESTAMP-SCAN`: target/build, gate `3/3`
  em `0.41 s`, recheck `3/3` em `0.13 s`, 30 processos, 12,000 parses e
  `2.00 s`. P95 ISO `783.3 ns`; maximo adversarial `178269.15 ns`, ou
  `0.178 ms`. O parser e O(n); nenhuma otimizacao foi aplicada.
- CTest sequencial `595/595` em `66.86 s`. O registro atual possui 596 testes
  porque o smoke de filename foi adicionado depois desse full.
- Security ampla: Semgrep `509` arquivos/`24` regras/zero finding, Gitleaks
  `1.42 GB`/zero leak e TruffleHog `9,226` chunks/zero segredo.
- Restam: profiling valido do prefetch por 1.0 ponto original, bloqueado pela
  ferramenta; tooling clang-tidy/Semgrep; `IMPORT-STAGER-HUB` em Windows/UNC;
  handles, PowerShell e packaging reais.
- Proxima atividade unica: canonizar clang-tidy macOS com sysroot e reproduzir
  IDs Semgrep. Depois, executar as provas externas enquanto profiling segue
  bloqueado.

## Historico anterior: atualizacao operacional - 2026-07-17

Fechamento posterior em 2026-07-18:

- HEAD de codigo `b2369ac`, precedido por `7291082`, `0025d44` e `7f396b0`,
  estabiliza os gates temporais, fecha o snapshot WAL e propaga
  `sqliteBusyWait` por recovery e journal cleanup.
- Em `4e74790`, CTest sequencial passou `581/581` em `82.06 s`; security ampla
  ficou limpa.
- Contadores: plano original `99.0/100`, divida nova `78.6/100` por 11/14 e
  backlog legado `0.0/100` sem denominador aceito.
- Controle operacional complementar: `2/8 = 25.0%`. Os dois pacotes SQLite
  atuais estao fechados. Determinismo e filename benchmark elevam a fila para
  `4/8 = 50.0%` e a divida nova para `13/14 = 92.9%`.
- Churn SQLite real: first read wall p50/p95 `6.948/12.897 ms`;
  open/read/close repetido `1.390/3.296 ms`; 2,400 chamadas concorrentes de
  `page()` ficaram em `157.501/219.458 ms`, sem falha ou cancelamento. Pool nao
  foi justificado e o cache de paginas do SO nao foi controlado.
- No marco intermediario `4e74790`, Bitbucket foi confirmado e GitLab ficou
  bloqueado por OAuth `invalid_grant`.
- A publicacao do rescan agora usa SQLite Backup API no destino. Os 3 REDs
  passaram `3/3` em `0.22 s`, o gate ampliado passou `7/7` em `0.35 s` e o
  review Sol fechou a corrida de stop tardio apos `SQLITE_DONE`.
- `EXPLAIN QUERY PLAN` confirmou que `COLLATE NOCASE` impede seek no indice
  BINARY de `numero_ssa`; count/rows cairam de `43/71 ms` para `2/<1 ms` no
  experimento read-only.
- `1cea684` restringe BINARY a SSA canonica; `3/3` e `45/45` passaram. Em 30
  processos, 2,400/2,400 reads foram corretos e `page()` p50/p95 ficou em
  `0.133/0.229 ms`, contra `157.501/219.458 ms`.
- `360d18b` retira QLockFile, I/O SQLite e log do mutex do resumo de derivadas.
  O RED falhou em `1.06 s`; o GREEN passou 2/2, 60 repeticoes, `13/13` e
  `8/8`. Sol xhigh e Terra high deram GO apos fechar 1 P2 de cobertura.
- Os oito testes do registro antigo de 564 casos passaram `8/8` em `10.09 s`;
  a matriz atual possui 588 casos.
- Na rodada atual, os oito nomes passaram `8/8` em `9.77 s`; os tres contratos
  SQLite passaram 30 execucoes e a familia ampliada passou `15/15` em `2.67 s`.
- `610fbf3` prova o snapshot WAL antigo durante publicacao e o novo snapshot na
  transacao seguinte.
- `7f396b0`: conversor 20/20, SAM 20/20 suites, Details 10/10, CurrentWeek
  20/20 e CTest final `588/588` em `78.30 s`. Semgrep, Gitleaks e TruffleHog
  amplos ficaram limpos.
- `7291082` foi confirmado no Bitbucket com divergencia `0/0`; o push GitLab
  continua bloqueado por OAuth `invalid_grant`.
- `b2369ac` fecha `SQLITE-SCHEMA-VERSION-GATE`: legado zero e versao atual sao
  aceitos, futuras falham antes de DDL/DML ou movimento de recovery, o stamp
  ocorre na write transaction e o validator usa snapshot de leitura unico.
- RED de recovery confirmado; contratos principais `80/80`, cleanup direto
  `30/30`, gate amplo `45/45` e CTest final `595/595` em `75.81 s`. A primeira
  matriz `590/594` foi investigada sem aceitar timeout: os quatro casos passaram
  isolados e `40/40` repeticoes antes do full limpo.
- Security atual: Semgrep 508 arquivos/24 regras, Gitleaks 1.42 GB e TruffleHog
  Git 9,090 chunks, sem finding. Bitbucket confirmou `b2369ac` com `0/0`;
  GitLab continua bloqueado por OAuth `invalid_grant`.
- Proxima atividade: fechar `TEST-DETERMINISM-DELTA` e depois executar o
  benchmark formal de filename. Profiling permanece o terceiro slice porque a
  ferramenta local ainda nao gerou evidencia valida.

Historico de marcos intermediarios anteriores ao fechamento acima:

- Commit `e0b5401` publicado em `origin` e `bitbucket`.
- Commit `41ecf36` tambem publicado em `origin` e `bitbucket`.
- Commit `c95a757` tambem publicado em `origin` e `bitbucket`.
- Commits estruturais `14485a9`, `627a075`, `2c7a35f` e `55c6bd8` foram
  publicados no Bitbucket. GitLab esta pendente por OAuth `invalid_grant`.
- Dois polls sincronos de `quickSector()` foram removidos; o resumo baseado em
  timer foi preservado.
- Dois polls sincronicos de `textFilter()` foram removidos; os resumos baseados
  em timer permaneceram.
- Um poll sincronico de `sector->quickSector()` foi removido; timers de resumo
  permaneceram.
- Build e CTest da suite `ssa_qt_filter_panel_tests`: `1/1`, `2.96 s`.
- Validacao adicional da suite: `1/1`, `5.74 s`.
- Validacao adicional final: `1/1`, `2.88 s`.
- O workflow hub foi fechado com resultado tipado para SAM e XLSX/chunks.
- O stager hub foi separado localmente: stager com discovery/copias owned e
  consolidator com moves post-commit; Windows/UNC real segue pendente.
- Gates: `11/11` focados e `453/453` completos em `65.75 s`.
- Contadores auditados: plano original `99.0/100`, divida nova `64.3/100`
  (9/14 aceitos) e backlog legado sem denominador definido.

---

## Baseline e invariantes

- Baseline funcional antes do commit documental: `d34f92d` em `master`.
- Suite local autoritativa: 443/443 testes verdes.
- Schema SQLite e chaves internas permanecem ASCII canonico.
- Nenhum movimento de fonte pode ocorrer antes de banco e journal duraveis.
- N3 permanece fail-closed sem prova positiva do SO.
- Nenhum layout, label ou action ID muda sem requisito explicito.
- Untracked locais, configuracoes pessoais e artefatos nao entram no Git.

### Task 1: Triar feedback de Cursor GLM e OpenCode

**Files:**

- Modify: `ROUND_STATUS.md`
- Modify: `RECOVERY_BACKLOG.md`
- Reference: `docs/plans/2026-07-17-v0.9.10-follow-up.md`

#### Step 1: Registrar cada finding sem editar codigo

Criar uma tabela temporaria no report da rodada com origem, arquivo/linha,
severidade alegada, contrato afetado e evidencia fornecida.

#### Step 2: Reproduzir ou inspecionar no HEAD

Usar `rg`, testes existentes e o menor reproducer focado. Nao aceitar finding
por autoridade da ferramenta.

#### Step 3: Classificar

Marcar cada item como valido, duplicado, obsoleto, falso positivo, dependencia
externa ou fora de escopo. Findings validos entram no backlog com prioridade,
aceite e arquivos previstos.

#### Step 4: Atualizar os dois documentos de controle

`ROUND_STATUS.md` recebe fatos da rodada; `RECOVERY_BACKLOG.md` recebe somente
trabalho residual. Nao duplicar itens resolvidos.

#### Step 5: Review documental

Run: `markdownlint ROUND_STATUS.md RECOVERY_BACKLOG.md docs/plans/2026-07-17-v0.9.10-follow-up.md`

Expected: PASS.

### Task 2: Completar pointer real por familia de menu

**Files:**

- Modify: `tests/smoke/AdvancedPopupQmlTest.cpp`
- Modify: `tests/smoke/PresentationSmokeTest.cpp`
- Modify only if a defect is proven: `app/desktop/qml/Main.qml`
- Modify only if a defect is proven: `app/desktop/qml/components/SsaTable.qml`

#### Step 1: Mapear familias ainda sem pointer

Cobrir importacao/exportacao, manutencao/banco, header e grafo. Reusar testes de
handler existentes sem duplicar geometria para cada action.

#### Step 2: Escrever um teste RED por familia

Cada teste deve abrir o menu com mouse real, clicar a action e observar request
ou estado terminal, sem `invokeMethod("click")` no contrato de pointer.

#### Step 3: Executar teste focado

Run: `ctest --preset dev --output-on-failure -R "ssa_qml_advanced_popup_tests|ssa_qt_presentation_tests"`

Expected: FAIL somente no contrato novo antes da correcao.

#### Step 4: Implementar o minimo se houver defeito de producao

Preservar labels, IDs, layout e `Theme.clampedPopupX/Y`.

#### Step 5: Medir offscreen e remover instrumentacao

Run: `QT_QPA_PLATFORM=offscreen QT_FATAL_WARNINGS=1 ctest --preset dev --output-on-failure -R "ssa_qml_advanced_popup_tests|ssa_qt_presentation_tests"`

Expected: PASS sem `console.info` residual.

### Task 3: Fechar contrato multiplataforma do grafo

**Files:**

- Modify: `src/presentation/DerivadasGraphModel.cpp`
- Modify: `src/presentation/DerivadasGraphModel.h`
- Modify: `app/desktop/qml/components/DerivadasGraph.qml`
- Test: `tests/smoke/AdvancedPopupQmlTest.cpp`

#### Step 1: Escrever casos RED para fonte unica de geometria

Provar que hit-test, arestas, bounds e exportacao usam as mesmas dimensoes de
node sem duplicar `118x48` em C++ e QML.

#### Step 2: Escrever casos de URL

Cobrir `file:///`, espacos, Unicode, Windows drive e UNC sem fallback manual
silencioso.

#### Step 3: Implementar a menor fronteira Qt adequada

Conversao de `QUrl` fica na fronteira Qt; layout continua em presentation e
nao migra para domain.

#### Step 4: Validar modelo real e exportacao

Run: `QT_QPA_PLATFORM=offscreen QT_FATAL_WARNINGS=1 ctest --preset dev --output-on-failure -R "ssa_qml_advanced_popup_tests"`

Expected: PASS para determinismo, bounds, overlap, teclado, SVG e PNG.

### Task 4: Isolar benchmark de prefetch

**Files:**

- Modify: `tests/smoke/PresentationSmokeTest.cpp`
- Create only if measurement cannot stay in the suite: `tools/prefetch_benchmark.cpp`
- Modify only if a target is required: `CMakeLists.txt`

#### Step 1: Medir baseline sem otimizar

Separar startup, latencia ate `succeeded`, CPU ativa, idle, requests e RSS em
30 amostras. Registrar mediana e p95.

#### Step 2: Provar contratos funcionais

Cold page 1, cache hit 2/3, generation/fingerprint, cancelamento e latest-wins
devem permanecer verdes.

#### Step 3: Habilitar profiling somente no target dedicado

Corrigir QML debugging sem habilita-lo no release. Um trace danificado nao e
resultado valido.

#### Step 4: Decidir com numeros

Criar slice de otimizacao somente se a regressao exceder o ruido das amostras.

### Task 5: Reparar gates incompletos

**Files:**

- Modify only after diagnosis: `CMakeLists.txt`
- Modify only after diagnosis: `CMakePresets.json`
- Modify only after diagnosis: `.clang-tidy`

#### Step 1: Reproduzir clang-tidy sem editar

Run: `clang-tidy -p build/dev src/domain/ColumnCatalog.cpp`

Expected: o baseline atual falha ao localizar headers da toolchain; capturar o
comando de compilacao exato.

#### Step 2: Corrigir compile database/toolchain

Nao adicionar includes ao codigo para mascarar configuracao incorreta.

#### Step 3: Gerar `.qmltypes` real

Run: `cmake --build --preset dev --target all_qmllint`

Expected: PASS sem aviso de `ssa_consulta_rapida.qmltypes` ausente.

#### Step 4: Revalidar Semgrep grande

Separar fixtures do arquivo de integracao somente se isso eliminar timeout sem
reduzir cobertura das duas regras narrow/wide.

### Task 6: Desenhar hardening TOCTOU da consolidacao

**Files:**

- Review: `src/infra/import/ImportFileStager.cpp`
- Review: `src/infra/import/SpreadsheetImportWorkflowPort.cpp`
- Review: `tests/integration/SpreadsheetImportWorkflowPortTests.cpp`
- Modify after design approval only: platform-specific consolidation files

#### Step 1: Criar reproducer adversarial controlado

Trocar o diretorio entre validacao e rename sem tocar banco real.

#### Step 2: Comparar primitives por plataforma

Avaliar `openat`/`renameat` em POSIX e handles equivalentes em Windows. Nao
introduzir wrapper generico antes de provar o contrato comum.

#### Step 3: Apresentar desenho antes da implementacao

Parar se a solucao exigir ownership cross-thread, reaper ou API publica nova.

### Task 7: Gate e publicacao do proximo slice

**Files:**

- Modify: `ROUND_STATUS.md`
- Modify: `RECOVERY_BACKLOG.md`

#### Step 1: Review imediato

Executar clang-format/cppcheck/Semgrep/detect-secrets por diff. Qmlformat
somente quando QML mudar; CodeRabbit, gitleaks e TruffleHog uma vez no
fechamento.

#### Step 2: Build e testes

Run: `cmake --build --preset dev`

Run: `ctest --preset dev --output-on-failure`

Expected: 100% PASS; timeout nunca conta como sucesso.

#### Step 3: Atualizar controle

Registrar HEAD, suite real, working tree, remotes, validacao local e
dependencias externas sem misturar os estados.

#### Step 4: Commit e push somente com autorizacao

Publicar `master` em `origin` e `bitbucket`; nao publicar em `gh` enquanto a
conta estiver suspensa.
